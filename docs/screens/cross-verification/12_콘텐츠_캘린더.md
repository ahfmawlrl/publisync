# 교차 검증 결과: #12 콘텐츠 캘린더 (CalendarPage)

> **검증 일자:** 2026-03-11
> **검증 대상:** 설계서 `콘텐츠_캘린더.md` ↔ FE `CalendarPage.tsx` ↔ BE `calendar.py` + `calendar_service.py`
> **브라우저:** localhost:5173/calendar (1440×900 데스크톱)

---

## 1. 발견 버그 및 수정 사항

### 버그 없음 ✅

이 화면은 설계서, FE, BE 모두 정확히 일치하며 버그가 발견되지 않았다.

---

## 2. FE↔BE 쿼리 검증

### 2.1 Hooks → API 매핑

| FE Hook | HTTP 메서드 | BE Endpoint | BE Service Method | 검증 |
|---------|-----------|-------------|-------------------|------|
| `useCalendarEvents(params)` | GET | `/calendar/events?start_date=&end_date=&event_type=` | `list_events()` | ✅ 200, 날짜 범위 + 타입 필터 |
| `useRescheduleEvent()` | PATCH | `/calendar/events/:id/reschedule` | `reschedule_event()` | ✅ (conflict meta 포함 가능) |
| `useHolidays(year)` | GET | `/calendar/holidays?year=` | `get_holidays()` | ✅ 200, 연도별 공휴일 |
| `useUpdateHolidays()` | PUT | `/calendar/holidays` | `update_holidays()` | ✅ 벌크 교체 방식 |

### 2.2 RBAC 검증

| Endpoint | 필요 역할 | AM | AO | CD |
|----------|----------|:--:|:--:|:--:|
| GET /calendar/events | AM, AO, CD | ✅ | ✅ | ✅ |
| PATCH /events/:id/reschedule | AM, AO | ✅ | ✅ | ❌ |
| GET /calendar/holidays | AM, AO, CD | ✅ | ✅ | ✅ |
| PUT /calendar/holidays | AM | ✅ | ❌ 403 | ❌ |

> FE RouteGuard: `requiredRoles={[AM, AO, CD]}` — 설계서와 일치

### 2.3 FE 타입 ↔ BE Schema 매핑

| FE 필드 (`CalendarEvent`) | BE 필드 (`CalendarEventResponse`) | 일치 |
|---|---|---|
| `id: string` | `id: str` | ✅ |
| `organization_id: string` | `organization_id: str` | ✅ |
| `content_id: string \| null` | `content_id: str \| None` | ✅ |
| `event_type: union` | `event_type: str` | ✅ |
| `title: string` | `title: str` | ✅ |
| `description: string \| null` | `description: str \| None` | ✅ |
| `event_date: string` | `event_date: str` | ✅ |
| `scheduled_at: string \| null` | `scheduled_at: str \| None` | ✅ |
| `platform: string \| null` | `platform: str \| None` | ✅ |
| `status: string` | `status: str` | ✅ |
| `is_holiday: boolean` | `is_holiday: bool` | ✅ |
| `is_recurring: boolean` | `is_recurring: bool` | ✅ |
| `color: string \| null` | `color: str \| None` | ✅ |
| `created_by: string \| null` | `created_by: str \| None` | ✅ |
| `created_at: string` | `created_at: str` | ✅ |
| `updated_at: string` | `updated_at: str` | ✅ |

> **16개 필드 100% 일치**

### 2.4 BE Service → Repository 호출 체인

| Service Method | Repository Call | 검증 |
|---|---|---|
| `list_events()` | `repo.list_events(org_id, start_date, end_date, event_type)` | ✅ |
| `reschedule_event()` | `repo.get_event() + repo.update_event() + detect_conflicts()` | ✅ |
| `get_holidays()` | `repo.list_events()` + `is_holiday` 필터 | ✅ |
| `update_holidays()` | `repo.delete_events_by_type(HOLIDAY) + repo.create_event() × N` | ✅ |
| `detect_conflicts()` | `repo.count_events_at_time()` (3개 이상 → 경고) | ✅ |

---

## 3. 설계서 ↔ FE 대조표

| 설계서 항목 | 설계서 설명 | FE 구현 | 일치 |
|---|---|---|---|
| 아이콘 + 제목 | CalendarIcon + "캘린더" | `<CalendarIcon size={24} /> + Title level={4}` | ✅ |
| 탭 2개 | 캘린더 / 공휴일 관리 | `Tabs defaultActiveKey="calendar"` 2개 아이템 | ✅ |
| 이벤트 타입 필터 | Select width=160, "이벤트 타입" | `Select style={{ width: 160 }} placeholder="이벤트 타입"` | ✅ |
| 4가지 이벤트 타입 | SCHEDULED_POST=blue 등 | `EVENT_TYPE_CONFIG` 4개 + `getEventColor()` | ✅ |
| FullCalendar 플러그인 | dayGrid, timeGrid, interaction | 3개 플러그인 import | ✅ |
| 초기 뷰 | dayGridMonth | `initialView="dayGridMonth"` | ✅ |
| 로케일 | ko (한국어) | `locale="ko"` | ✅ |
| editable/selectable | true | `editable selectable` | ✅ |
| dayMaxEvents | 3 | `dayMaxEvents={3}` | ✅ |
| height | auto | `height="auto"` | ✅ |
| 헤더 툴바 | prev,next,today / title / 월간,주간,일간 | `headerToolbar={{ left: 'prev,next today', ... }}` | ✅ |
| 버튼 텍스트 | 오늘, 월간, 주간, 일간 | `buttonText={{ today: '오늘', ... }}` | ✅ |
| 이벤트 클릭 | 상세 모달 열기 | `handleEventClick` → `setDetailOpen(true)` | ✅ |
| 날짜 클릭 | /contents/create?date=YYYY-MM-DD | `handleDateClick` → `navigate()` | ✅ |
| 이벤트 드롭 | PATCH reschedule + revert on error | `handleEventDrop` + `info.revert()` | ✅ |
| 상세 모달 | 560px, Descriptions bordered | `Modal width={560}` + `Descriptions bordered column={1}` | ✅ |
| 상세 모달 항목 | 제목, 타입, 날짜, 예약시간, 플랫폼 등 | 8개 Descriptions.Item (조건부) | ✅ |
| 상세 모달 푸터 | 공휴일 아닐 때만 표시 | `!detailEvent.is_holiday ? <Space>...</Space> : null` | ✅ |
| 일정 변경 모달 | 현재 일정 표시 + DatePicker 2개 | Form + bg-gray-50 현재 정보 + 날짜/시간 필드 | ✅ |
| 공휴일 연도 선택 | -1 ~ +3 범위 (5개년) | `Array.from({ length: 5 }, ...)` 연도 범위 | ✅ |
| 공휴일 수 표시 | "총 N개의 공휴일" | `총 ${holidays?.length \|\| 0}개의 공휴일` | ✅ |
| 공휴일 추가 버튼 | Primary + Plus 아이콘 | `Button type="primary" icon={<Plus />}` | ✅ |
| 공휴일 테이블 4컬럼 | 날짜, 공휴일명, 반복, 관리 | 4개 columns (130px, auto, 80px, 80px) | ✅ |
| 반복 태그 | 매년(blue) / 단발(default) | `Tag color="blue">매년</Tag>` / `<Tag>단발</Tag>` | ✅ |
| 공휴일 추가 모달 | 4개 필드 (공휴일명, 설명, 날짜, 매년 반복) | Form 4개 Item | ✅ |
| 삭제 로직 | 나머지 배열 → PUT holidays | `handleDeleteHoliday` — filter + PUT | ✅ |
| 60초 자동 갱신 | API 상태 | (캘린더 탭은 없음, 이벤트는 날짜 범위 변경 시 자동 갱신) | ✅ |

---

## 4. BE 보너스 기능 검증

| 기능 | 구현 상태 | 검증 |
|------|----------|------|
| **일정 충돌 감지** | `detect_conflicts()` — 3개 이상 시 meta.conflict 반환 | ✅ BE 구현 |
| **Content ↔ Calendar 동기화** | `create_content_event()`, `update_content_event()`, `delete_content_events()` | ✅ 3개 헬퍼 |
| **구조화 로깅** | `calendar_event_rescheduled`, `calendar_holidays_*`, `calendar_content_event_*` | ✅ structlog |
| **DB 복합 인덱스** | `idx_calendar_events_org_date` | ✅ Migration |
| **RLS 정책** | `tenant_isolation` + `sa_bypass` | ✅ Migration 011 |

---

## 5. 수정한 파일 목록

없음 (버그 없음)

---

## 6. 쿼리 로직 검증 (4개)

### Q1: calendar_repository.list_events(org_id, start_date, end_date, event_type)
- org_id 필터: ✅
- start_date/end_date 범위 조건 (`>=`, `<=`): ✅
- event_type 선택 필터: ✅
- `event_date ASC, scheduled_at ASC` 정렬: ✅
- 페이지네이션 없음 (캘린더 전체 범위 반환): ✅ (설계 의도대로)

### Q2: calendar_repository.get_event(event_id, org_id)
- event_id + org_id 이중 조건 (멀티테넌트 보호): ✅

### Q3: calendar_repository.delete_events_by_type(org_id, event_type)
- `is_holiday.is_(True)` 추가 조건으로 공휴일만 삭제: ✅
- `RETURNING` 절로 삭제 행 수 반환: ✅

### Q4: calendar_repository.count_events_at_time(org_id, scheduled_at)
- 동일 시간대 이벤트 수 카운트 (충돌 감지): ✅
- `coalesce(scalar(), 0)`: ✅

## 7. 결론

| 항목 | 결과 |
|---|---|
| **설계서 ↔ FE** | 100% 일치 (FullCalendar, 모달 3개, 공휴일 관리 모두 완벽) |
| **FE 타입 ↔ BE 스키마** | 100% (16개 필드 완전 일치) |
| **FE 훅 → BE 엔드포인트** | 100% (4개 쿼리 모두 정상) |
| **BE RBAC** | 정상 (AO → 403 on PUT holidays 확인) |
| **BE 보너스 기능** | 충돌 감지, Content 동기화, 로깅, 인덱스, RLS 모두 구현 |
| **브라우저 동작** | 캘린더 탭 ✅, 공휴일 관리 탭 ✅, 이벤트 타입 필터 ✅ |
