# #29 자막 편집기 — 교차 검증 결과

## 검증 대상
- 설계서: `docs/screens/자막_편집기.md`
- FE: `features/ai/pages/SubtitleEditorPage.tsx` (360행)
- FE Hook: `features/ai/hooks/useAiJobs.ts` — useCreateSubtitles, useSaveSubtitles, useJobStatus
- FE Components: `features/ai/components/AiJobProgress.tsx`, `shared/components/VideoPlayer.tsx`, `shared/components/WaveformViewer.tsx`
- BE Router: `api/v1/ai.py` — POST /generate-subtitles

## API 엔드포인트 (관련 3개)

| Method | Path | RBAC | FE Hook | 상태 |
|--------|------|------|---------|------|
| POST | /ai/generate-subtitles | AM,AO | useCreateSubtitles | ✅ |
| GET | /ai/jobs/:jobId | AM,AO | useJobStatus (polling) | ✅ |
| PUT | /media/:id/subtitles | AM,AO | useSaveSubtitles | ✅ |

## 설계서 대비 구현 확인

| 항목 | 설계서 | 구현 | 일치 |
|------|--------|------|------|
| 2컬럼 레이아웃 (60/40) | 좌: 비디오+파형, 우: 자막 | grid-cols-5 (3:2) | ✅ |
| AI 자막 생성 버튼 | POST → 202 → polling | useCreateSubtitles + AiJobProgress | ✅ |
| 자막 편집 (시간/텍스트) | SRT 형식 start/end/text | SubtitleEntry 인터페이스 | ✅ |
| 자막 추가/삭제 | 수동 추가, 재인덱싱 | handleAddEntry/handleRemoveEntry | ✅ |
| SRT 내보내기 | Blob 다운로드 | generateSrt + downloadAsFile | ✅ |
| 서버 저장 | PUT /media/:id/subtitles | useSaveSubtitles | ✅ |
| Human-in-the-Loop | AI 제안 → 사용자 편집 → 확정 | ✅ | ✅ |
| Fallback | AI 실패 시 수동 추가 | Empty + "수동으로 추가하세요" | ✅ |

## SRT 포맷 유틸리티 검증
- `srtTimeToSeconds("01:02:03,456")` → 3723.456: ✅
- `secondsToSrtTime(3723.456)` → "01:02:03,456": ✅
- `generateSrt()` → 표준 SRT 형식 출력: ✅

## 브라우저 렌더링
- 제목 "자막 편집기" + 3 버튼 (AI 자막 생성, 서버에 저장, SRT 내보내기): ✅
- 좌측: VideoPlayer + WaveformViewer: ✅
- 우측: 자막 목록 (빈 상태 "자막이 없습니다"): ✅

## 쿼리 로직 검증 (3개)

### Q1: ai_usage_repository — POST /ai/generate-subtitles
- `AiJob` INSERT: org_id + job_type(SUBTITLE) + status(PENDING) + input_params(JSON): ✅
- Celery 비동기 디스패치 (ai 큐): ✅
- 응답 202 + job_id 반환: ✅

### Q2: ai_usage_repository — GET /ai/jobs/:jobId
- `AiJob.id == job_id` + `AiJob.organization_id == org_id` 이중 조건: ✅
- 멀티테넌트 격리 (타 기관 job 접근 불가): ✅
- status, progress, result 반환: ✅

### Q3: media_repository — PUT /media/:id/subtitles
- `MediaAsset.id == asset_id` + `organization_id == org_id`: ✅
- `deleted_at.is_(None)` 소프트 삭제 필터: ✅
- metadata JSONB 업데이트 (subtitles 필드): ✅

## 최종 결과: 버그 없음 ✅
