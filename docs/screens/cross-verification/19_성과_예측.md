# 교차 검증 결과: #19 성과 예측 (PredictionPage)

> **검증 일자:** 2026-03-11
> **검증 대상:** 설계서 `성과_예측.md` ↔ FE `PredictionPage.tsx` ↔ BE `analytics.py`
> **브라우저:** localhost:5173/analytics/prediction (1440×900 데스크톱)

---

## 1. 발견 버그 및 수정 사항

### 🟢 버그 없음

---

## 2. FE↔BE 쿼리 검증

### 2.1 Hooks → API 매핑

| FE Hook | HTTP | BE Endpoint | 검증 |
|---------|------|-------------|------|
| `usePrediction(contentId?)` | GET | `/analytics/prediction?content_id=` | ✅ 200 |
| `useOptimalTime()` | POST | `/ai/optimal-time` | ✅ (AI 서비스, 별도 라우터) |
| contents 검색 (inline useQuery) | GET | `/contents?search=` | ✅ (콘텐츠 선택용) |

### 2.2 RBAC 역할별 검증

| 역할 | prediction | 결과 |
|------|:----------:|------|
| AM | 200 | ✅ |
| AO | 200 | ✅ |
| CD | 200 | ✅ |

### 2.3 쿼리 로직 검증

| Repository 메서드 | 검증 | 결과 |
|---|---|---|
| COUNT total posts (6개월) | cutoff_6m + org_id + SUCCESS 필터 | ✅ |
| AVG by platform (6개월) | cutoff_6m + JOIN Channel + GROUP BY platform | ✅ |
| Best hours (DOW+hour) | extract(dow/hour) + AVG engagement DESC LIMIT 5 | ✅ |
| Service `data_months` | `min(6, max(1, total//30))` 근사 계산 | ✅ |
| Service DOW_NAMES | PG DOW 기준 `{0:"SUN",1:"MON"...}` 올바름 | ✅ |

---

## 3. 설계서 ↔ FE 대조표

| 설계서 항목 | FE 구현 | 일치 |
|---|---|---|
| 제목 "콘텐츠 성과 예측" + BETA 태그 | `<Title>콘텐츠 성과 예측</Title> + <Tag color="blue">BETA</Tag>` | ✅ |
| 데이터 부족 안내 (3개월 미만) | `data.data_months < 3` → Empty + 안내 텍스트 | ✅ |
| 신뢰도 Progress 바 | `<Progress percent={confidence*100}/>` | ✅ |
| 학습 데이터 Statistic | `<Statistic title="학습 데이터" suffix="개월"/>` | ✅ |
| 플랫폼별 예상 성과 Table | 3열: 플랫폼/예상조회수/예상좋아요 (min~max) | ✅ |
| 최적 게시 시간 Table | 3열: 요일/시간대/근거 | ✅ |
| A/B 테스트 제안 Table | 5열: 항목/A/B/예측/근거 | ✅ |
| AI 최적 시간 추천 도구 | TextArea + Select(multi) + 추천받기 Button | ✅ |
| AI 추천 결과 | Card bg-blue-50 + Table + 신뢰도/모델 | ✅ |
| 좌우 2열 (14:10) | `<Col lg={14}>` / `<Col lg={10}>` | ✅ |
| Empty 상태 | "예측 데이터가 없습니다" / "최적 시간 데이터가 없습니다" | ✅ |

---

## 4. 타입 비교 (FE ↔ BE)

| 타입 | FE 필드수 | BE 필드수 | 일치 |
|------|----------|----------|------|
| PredictionData | 7 | 7 (PredictionResponse) | ✅ |
| PredictionPlatformItem | 3 | 3 | ✅ |
| PredictionRangeItem | 2 (min, max) | 2 | ✅ |
| OptimalTimeSlot | 3 | 3 | ✅ |
| AbTestSuggestion | 5 | 5 | ✅ |

---

## 5. 결론

| 항목 | 결과 |
|---|---|
| **설계서 ↔ FE** | 100% 일치 (11개 항목) |
| **FE 훅 → BE** | 100% (2개 API + 콘텐츠 검색) |
| **RBAC** | ✅ (전체 역할 접근 가능) |
| **타입 일치** | 100% (5개 타입) |
| **치명적 버그** | 0건 |
| **브라우저** | 데이터 부족 화면 정상 (0개월 → Empty 안내) |
