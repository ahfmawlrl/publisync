# 교차 검증 결과: #20 벤치마크 분석 (BenchmarkPage)

> **검증 일자:** 2026-03-11
> **검증 대상:** 설계서 `벤치마크_분석.md` ↔ FE `BenchmarkPage.tsx` ↔ BE `analytics.py`
> **브라우저:** localhost:5173/analytics/benchmark (1440×900 데스크톱)

---

## 1. 발견 버그 및 수정 사항

### 🟢 버그 없음

---

## 2. FE↔BE 쿼리 검증

### 2.1 Hooks → API 매핑

| FE Hook | HTTP | BE Endpoint | RBAC | 검증 |
|---------|------|-------------|------|------|
| `useBenchmark(period)` | GET | `/analytics/benchmark` | AM, CD | ✅ 200 |
| `useOrgComparison(period)` | GET | `/analytics/benchmark/organizations` | AM only | ✅ 200 |

### 2.2 RBAC 역할별 검증

| 역할 | benchmark | benchmark/organizations |
|------|:---------:|:----------------------:|
| AM | 200 ✅ | 200 ✅ |
| AO | 403 ✅ | 403 ✅ |
| CD | 200 ✅ | 403 ✅ |

---

## 3. 설계서 ↔ FE 대조표

| 설계서 항목 | FE 구현 | 일치 |
|---|---|---|
| PageHeader "벤치마크 분석" + 부제목 | `<PageHeader title="벤치마크 분석" subtitle="업종 평균..."/>` | ✅ |
| Segmented 7일/30일/90일 | `<Segmented options={[7d,30d,90d]}/>` | ✅ |
| 마지막 업데이트 표시 | `{benchmark.updated_at && <Text>마지막 업데이트: ...</Text>}` | ✅ |
| KPI 3열 (sm=8) | `<Col sm={8}>` × 3: 종합점수/분석플랫폼/업종 | ✅ |
| 종합 점수 (Award 아이콘) | `<Statistic prefix={<Award/>} value={score} suffix="/100"/>` | ✅ |
| 점수 색상 (70+ 초록, 40+ 노랑, 미만 빨강) | `color: score>=70 ? green : score>=40 ? orange : red` | ✅ |
| 플랫폼별 벤치마크 Table | 5열: 플랫폼/순위/총조회수(평균비교)/참여율(평균비교)/백분위(Progress) | ✅ |
| 조회수 비교 BarChart (12:12) | `<BarChart>` 내기관 vs 업종평균, `<Col lg={12}>` | ✅ |
| 성과 레이더 RadarChart | `<RadarChart>` percentile, `<Col lg={12}>` | ✅ |
| 기관 비교 Table | 6열: 기관명/플랫폼/조회수/좋아요/참여율/게시수 (sorter 포함) | ✅ |
| Empty 상태 | "벤치마크 데이터가 없습니다" / "비교할 기관 데이터가 없습니다" | ✅ |
| 로딩 상태 | Spin size="large", h-96 | ✅ |

---

## 4. 타입 비교 (FE ↔ BE)

| 타입 | FE 필드수 | BE 필드수 | 일치 |
|------|----------|----------|------|
| BenchmarkData / BenchmarkResponse | 5 | 5 | ✅ |
| BenchmarkPlatformItem | 4 | 4 | ✅ |
| BenchmarkMetric | 4 | 4 | ✅ |
| OrgComparisonData / OrgComparisonResponse | 2 | 2 | ✅ |
| OrgComparisonItem | 8 | 8 | ✅ |

---

## 5. 쿼리 로직 검증 (2개)

### Q1: analytics_repository.get_benchmark_data(org_id, start_date, end_date)
- `_temporarily_bypass_rls()` 호출로 RLS 우회 (교차 기관 평균 계산): ✅
- 자기 기관 데이터: `Channel.organization_id == org_id` 필터: ✅
- 업종 평균 데이터: 전체 기관 대상 `GROUP BY platform` 집계: ✅
- 백분위 계산: `(자기 지표 / 전체 지표) * 100` 정규화: ✅
- 종합 점수: 플랫폼별 백분위 가중 평균: ✅

### Q2: analytics_repository.get_org_comparison_data(org_id, start_date, end_date)
- `_temporarily_bypass_rls()` 호출로 RLS 우회 (교차 기관 비교): ✅
- 전체 기관 대상 `GROUP BY organization_id, platform`: ✅
- `JOIN Organization` for org_name: ✅
- SUM(views, likes, comments) + 참여율 계산: ✅
- `ORDER BY total_views DESC`: ✅

---

## 6. 결론

| 항목 | 결과 |
|---|---|
| **설계서 ↔ FE** | 100% 일치 (12개 항목) |
| **FE 훅 → BE** | 100% (2개 API) |
| **RBAC** | 100% (benchmark=AM,CD; orgs=AM only; AO=403) |
| **타입 일치** | 100% (5개 타입) |
| **치명적 버그** | 0건 |
| **브라우저** | PageHeader ✅, KPI ✅, Empty ✅, 2열차트 ✅, 기관비교 ✅ |
