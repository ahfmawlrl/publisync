# PubliSync 데이터베이스 설계서

> **버전:** v1.3
> **작성일:** 2026-03-11
> **상태:** 구현 완료 — DB 실사 교차 검증 반영

---

## 변경 이력

| 버전 | 날짜 | 작성자 | 변경 내용 |
|---|---|---|---|
| v1.0 | 2026-03-04 | Claude | 초안 작성 — 24개 테이블, 15개 ENUM, 14개 마이그레이션 |
| v1.1 | 2026-03-04 | Claude | 5개 설계 문서 교차 검증 반영 — CRITICAL 3건 + HIGH 11건 + MEDIUM 14건 수정 |
| v1.2 | 2026-03-04 | Claude | 구현 준비 점검 — channel_histories 추가, 역할 정책 확정, 상태 전이 규칙, 테이블명 통일. 31개 테이블, 19개 ENUM, 15개 마이그레이션 |
| v1.3 | 2026-03-11 | Claude | DB 실사 교차 검증 — 실제 스키마와 대조하여 32개 테이블, 25개 ENUM, 17개 마이그레이션으로 갱신. ai_jobs 테이블 추가, 6개 ENUM 추가, 테이블명·컬럼·ENUM 값·RLS 변수 불일치 전수 반영 |
| v2.0 | 2026-03-13 | Claude | 콘텐츠 워크플로우 재설계 — content_variants, variant_media 테이블 추가. mediaroletype ENUM 추가. contents에 source_media_id, publish_results에 variant_id, approval_histories에 variant_id 컬럼 추가. 34개 테이블, 26개 ENUM |

## 참조 문서

| 문서 | 버전 | 참조 섹션 |
|---|---|---|
| 프로젝트 범위 정의서 | v1.2 | §4-6 기능 정의, 상태 전이, Phase |
| 메뉴 구조 및 화면 설계서 | v1.2 | §4 화면 상세 — UI 필드 → 컬럼 검증 |
| API 설계서 | v1.2 | §4-7 엔드포인트 — 요청/응답 필드 → 컬럼 도출 |
| 기술 스택 결정서 | v1.2 | §3.3 PostgreSQL 16, UUID v7, JSONB, tsvector |
| 시스템 아키텍처 설계서 | v1.1 | §5 멀티테넌트, §8 비동기, §10 AI, §14 감사 |

---

## 1. 설계 원칙

### 1.1 8가지 핵심 원칙

| # | 원칙 | 설명 |
|---|---|---|
| 1 | **UUID v7 PK** | 시간 정렬 가능한 UUID v7을 모든 테이블 PK로 사용. `gen_random_uuid()` 대신 `uuid_generate_v7()` 사용 (pg_uuidv7 확장) |
| 2 | **RLS 기반 멀티테넌트** | `organization_id` FK를 가진 테이블에 PostgreSQL RLS 적용. `SET LOCAL app.current_org_id`로 세션 변수 설정 |
| 3 | **네이밍 규칙** | snake_case, 복수형 테이블명, `{table}_id` FK, `idx_{table}_{columns}` 인덱스 |
| 4 | **타임스탬프** | `created_at` DEFAULT NOW(), `updated_at` (트리거 자동 갱신), `deleted_at` (소프트 삭제) |
| 5 | **JSONB 활용** | 구조가 자주 변하는 필드에 JSONB 사용 (metadata, settings, ai_result 등). GIN 인덱스 적용 |
| 6 | **ENUM 타입** | PostgreSQL `CREATE TYPE`으로 정의. SQLAlchemy `Enum` 매핑. 값 추가만 허용 (삭제/변경 X) |
| 7 | **인덱스 전략** | 모든 FK에 B-tree, 빈번한 필터 컬럼에 복합 인덱스, JSONB에 GIN, tsvector에 GIN |
| 8 | **Phase별 점진 확장** | 테이블/컬럼 추가만 수행. 기존 스키마 변경 최소화. 마이그레이션은 스프린트와 1:1 대응 |

### 1.2 공통 타임스탬프 트리거

```sql
-- updated_at 자동 갱신 트리거 함수
CREATE OR REPLACE FUNCTION trigger_set_updated_at()
RETURNS TRIGGER AS $$
BEGIN
  NEW.updated_at = NOW();
  RETURN NEW;
END;
$$ LANGUAGE plpgsql;

-- 각 테이블에 적용
CREATE TRIGGER set_updated_at
  BEFORE UPDATE ON {table_name}
  FOR EACH ROW
  EXECUTE FUNCTION trigger_set_updated_at();
```

### 1.3 UUID v7 확장

```sql
-- pg_uuidv7 확장 설치
CREATE EXTENSION IF NOT EXISTS pg_uuidv7;

-- 대안: uuid-ossp + pgcrypto (pg_uuidv7 미지원 시)
CREATE EXTENSION IF NOT EXISTS "uuid-ossp";
CREATE EXTENSION IF NOT EXISTS pgcrypto;
```

---

## 2. ENUM 타입 정의

26개 PostgreSQL ENUM 타입.

> **RLS 세션 변수:** SA 우회 정책에 `current_setting('app.user_role', true) = 'SYSTEM_ADMIN'` 사용.
> 미들웨어에서 `SET LOCAL app.user_role = :role`을 설정한다.
> (v1.2까지 `app.user_role`로 표기되었으나, 실제 구현은 `app.user_role`이며 v1.3에서 통일.)

> **사용자 역할 정책 (다중 기관 지원):**
> - `users.role`은 **기본(글로벌) 역할**. 최초 가입/초대 시 설정. SA는 항상 글로벌.
> - `user_organizations.role`은 **기관별 역할**. 동일 사용자가 기관마다 다른 역할 가능.
> - **워크스페이스 전환 시** RBAC 미들웨어는 `user_organizations.role`을 사용:
>   ```python
>   # Workspace 미들웨어 역할 결정 로직
>   uo = await db.get(UserOrganization, user_id=user.id, organization_id=workspace_id)
>   effective_role = uo.role if uo else user.role  # fallback to global
>   await session.execute(text("SET LOCAL app.user_role = :role"), {"role": effective_role})
>   ```
> - API 응답의 `role` 필드는 **현재 워크스페이스 기준 역할**을 반환.
> - SA 역할은 `user_organizations`에 관계없이 전체 접근 (RLS `sa_bypass` 정책).

### Phase 1-A ENUM (10개)

```sql
-- 사용자 역할
CREATE TYPE user_role AS ENUM (
  'SYSTEM_ADMIN',      -- SA: 시스템 관리자
  'AGENCY_MANAGER',    -- AM: 수탁업체 관리자
  'AGENCY_OPERATOR',   -- AO: 수탁업체 실무자
  'CLIENT_DIRECTOR'    -- CD: 위탁기관 담당자
);

-- 사용자 상태
CREATE TYPE user_status AS ENUM (
  'ACTIVE',            -- 활성
  'INACTIVE',          -- 비활성
  'LOCKED',            -- 잠금 (5회 로그인 실패)
  'WITHDRAWN'          -- 탈퇴
);

-- 기관 상태
CREATE TYPE org_status AS ENUM (
  'ACTIVE',            -- 활성
  'INACTIVE',          -- 비활성
  'SUSPENDED'          -- 정지
);

-- 기관 플랜
CREATE TYPE org_plan AS ENUM (
  'FREE',
  'BASIC',
  'PRO',
  'ENTERPRISE'
);

-- 초대 상태
CREATE TYPE invitation_status AS ENUM (
  'PENDING',           -- 대기
  'ACCEPTED',          -- 수락
  'EXPIRED',           -- 만료
  'REVOKED'            -- 취소
);

-- 플랫폼 유형
CREATE TYPE platform_type AS ENUM (
  'YOUTUBE',
  'INSTAGRAM',
  'FACEBOOK',
  'X',
  'NAVER_BLOG'
);

-- 채널 이벤트 유형 (실제 DB 반영)
CREATE TYPE channeleventtype AS ENUM (
  'CONNECTED',           -- 최초 연동
  'DISCONNECTED',        -- 연동 해제
  'TOKEN_REFRESHED',     -- 토큰 갱신
  'TOKEN_EXPIRED',       -- 토큰 만료
  'STATUS_CHANGED'       -- 상태 변경 (기타)
  -- v1.2의 RECONNECTED, RATE_LIMITED는 구현에서 제외
);

-- 채널 상태 (실제 DB 반영)
CREATE TYPE channelstatus AS ENUM (
  'DISCONNECTED',      -- 연동 해제
  'ACTIVE',            -- 정상 연동
  'EXPIRING',          -- 토큰 만료 임박 (7일 이내)
  'EXPIRED'            -- 토큰 만료
  -- v1.2의 TOKEN_EXPIRED→EXPIRED, RATE_LIMITED/RECONNECTING 제외
);

-- 콘텐츠 상태 (실제 DB 반영)
CREATE TYPE contentstatus AS ENUM (
  'DRAFT',               -- 임시 저장
  'PENDING_REVIEW',      -- 검토 요청 (승인 대기)
  'IN_REVIEW',           -- 검수 중 (검수자 확인 중)
  'APPROVED',            -- 승인 완료
  'REJECTED',            -- 반려
  'SCHEDULED',           -- 예약 게시 대기
  'PUBLISHING',          -- 게시 중
  'PUBLISHED',           -- 게시 완료
  'PARTIALLY_PUBLISHED', -- 일부 플랫폼만 성공
  'PUBLISH_FAILED',      -- 게시 실패 (v1.2 FAILED → 변경)
  'CANCELLED',           -- 예약 취소
  'ARCHIVED'             -- 보관됨 (v1.2 DELETED → 변경, soft-delete와 구분)
);

-- content_status 상태 전이 규칙
-- ┌──────────────────────────────────────────────────────────────┐
-- │  DRAFT ──→ PENDING_REVIEW ──→ IN_REVIEW ──→ APPROVED       │
-- │    ↑            │                  │            │            │
-- │    │            │                  │            ↓            │
-- │    │            │                  │        SCHEDULED ──→ PUBLISHING
-- │    │            │                  │            │         ↙     ↘
-- │    │            │                  ↓            │   PUBLISHED  FAILED
-- │    │            └──────────→ REJECTED           │       ↓
-- │    │                           │                │  PARTIALLY_PUBLISHED
-- │    └───────────────────────────┘                │
-- │                                                 ↓
-- │                                            CANCELLED
-- │  (모든 상태 → DELETED: soft delete)                         │
-- └──────────────────────────────────────────────────────────────┘
--
-- 허용 전이:
--   DRAFT           → PENDING_REVIEW, DELETED
--   PENDING_REVIEW  → IN_REVIEW, REJECTED, DELETED
--   IN_REVIEW       → APPROVED, REJECTED
--   REJECTED        → DRAFT (수정 후 재요청)
--   APPROVED        → SCHEDULED, PUBLISHING, DELETED
--   SCHEDULED       → PUBLISHING, CANCELLED
--   PUBLISHING      → PUBLISHED, PARTIALLY_PUBLISHED, FAILED
--   PUBLISHED       → DELETED
--   PARTIALLY_PUBLISHED → DELETED (일부 재시도는 publish_results 레벨)
--   FAILED          → PUBLISHING (재시도), DELETED
--   CANCELLED       → DRAFT (재활용), DELETED
--
-- PARTIALLY_PUBLISHED 판정 로직:
--   게시 완료 후 해당 content의 모든 publish_results를 집계:
--   - 모두 PUBLISHED   → contents.status = 'PUBLISHED'
--   - 모두 FAILED      → contents.status = 'FAILED'
--   - 혼재 (일부 성공) → contents.status = 'PARTIALLY_PUBLISHED'
--   Celery 콜백에서 자동 판정 (publish_content_callback 태스크)

-- 게시 결과 상태 (실제 DB 반영)
CREATE TYPE publishresultstatus AS ENUM (
  'PENDING',           -- 대기
  'SUCCESS',           -- 성공
  'FAILED'             -- 실패
);

-- 승인 액션 (실제 DB 반영)
CREATE TYPE approvalaction AS ENUM (
  'SUBMIT',            -- 제출
  'APPROVE',           -- 승인
  'REJECT',            -- 반려
  'REQUEST_CHANGES'    -- 수정 요청 (v1.2 REQUEST_REVISION → 변경)
);
```

### Phase 1-A 추가 ENUM (3개, 구현 시 추가)

```sql
-- 승인 요청 상태 (approval_requests.status 전용)
CREATE TYPE approvalstatus AS ENUM (
  'PENDING_REVIEW',    -- 승인 대기
  'IN_REVIEW',         -- 검토 중
  'APPROVED',          -- 승인됨
  'REJECTED'           -- 반려됨
);

-- 캘린더 이벤트 유형 (calendar_events.event_type 전용)
CREATE TYPE calendareventtype AS ENUM (
  'SCHEDULED_POST',    -- 예약 게시
  'HOLIDAY',           -- 공휴일
  'ANNIVERSARY',       -- 기념일
  'CUSTOM'             -- 사용자 정의
);

-- 리포트 상태 (reports.status 전용)
CREATE TYPE reportstatus AS ENUM (
  'GENERATING',        -- 생성 중
  'DRAFT',             -- 초안
  'FINALIZED'          -- 확정
);
```

### Phase 1-B ENUM (6개)

```sql
-- 알림 유형 (API와 통일)
CREATE TYPE notification_type AS ENUM (
  'PUBLISH_COMPLETE',  -- 게시 완료
  'PUBLISH_FAILED',    -- 게시 실패
  'APPROVAL_REQUEST',  -- 승인 요청
  'APPROVAL_RESULT',   -- 승인/반려 결과
  'DANGEROUS_COMMENT', -- 위험 댓글
  'COMMENT_NEW',       -- 새 댓글
  'TOKEN_EXPIRING',    -- 토큰 만료 임박
  'SYSTEM'             -- 시스템 공지
);

-- 알림 채널
CREATE TYPE notification_channel AS ENUM (
  'IN_APP',            -- 인앱 (SSE)
  'EMAIL',             -- 이메일
  'WEB_PUSH',          -- 웹 푸시
  'TELEGRAM'           -- 텔레그램
);

-- 댓글 감성
CREATE TYPE comment_sentiment AS ENUM (
  'POSITIVE',
  'NEUTRAL',
  'NEGATIVE',
  'DANGEROUS'
);

-- 댓글 상태
CREATE TYPE comment_status AS ENUM (
  'UNPROCESSED',       -- 미처리 (수집 직후)
  'PUBLISHED',         -- 게시 상태 (정상)
  'HIDDEN',            -- 숨김
  'PENDING_DELETE',    -- 삭제 승인 대기
  'DELETED'            -- 삭제됨
);

-- 감사 액션 (resource_type + action으로 API의 CONTENT_CREATE 등을 조합)
CREATE TYPE audit_action AS ENUM (
  'CREATE',
  'READ',
  'UPDATE',
  'DELETE',
  'PUBLISH',
  'APPROVE',
  'REJECT',
  'LOGIN',
  'LOGOUT',
  'INVITE',
  'EXPORT',
  'CONNECT',
  'DISCONNECT'
);

-- AI 작업 유형 (아키텍처 설계서의 11+ AI 기능 반영)
CREATE TYPE ai_task_type AS ENUM (
  'TITLE',             -- 제목 생성 (F02)
  'DESCRIPTION',       -- 설명문 생성 (F02)
  'HASHTAG',           -- 해시태그 생성 (F02)
  'META_DESC',         -- 메타 설명 (F02)
  'ALT_TEXT',          -- 대체 텍스트 (F02)
  'SENTIMENT',         -- 감성 분석 (F04)
  'COMMENT_REPLY',     -- 댓글 답글 (F05)
  'TONE_CONVERT',      -- 톤앤매너 변환 (F17)
  'CONTENT_REVIEW',    -- 표현 검수 (F21)
  'SUBTITLE',          -- 자막 생성 STT (F03)
  'SHORTFORM',         -- 숏폼 추출 (F15)
  'THUMBNAIL',         -- 썸네일 분석 (F16)
  'TRANSLATION',       -- 번역 (F22)
  'REPORT',            -- 리포트 생성 (F19)
  'PREDICTION'         -- 성과 예측 (F20)
);
```

### Phase 1-B 추가 ENUM (2개, 구현 시 추가)

```sql
-- AI 비동기 작업 유형 (ai_jobs.job_type)
CREATE TYPE aijobtype AS ENUM (
  'SUBTITLE',          -- 자막 생성
  'SHORTFORM',         -- 숏폼 추출
  'REPORT'             -- 리포트 생성
);

-- AI 비동기 작업 상태 (ai_jobs.status)
CREATE TYPE aijobstatus AS ENUM (
  'PENDING',           -- 대기
  'PROCESSING',        -- 처리 중
  'COMPLETED',         -- 완료
  'FAILED'             -- 실패
);
```

### Phase 2+ ENUM (3개)

```sql
-- 미디어 유형
CREATE TYPE mediatype AS ENUM (
  'IMAGE',
  'VIDEO',
  'AUDIO',
  'DOCUMENT'
);

-- 리포트 기간 (실제 DB 반영: DAILY 제외)
CREATE TYPE reportperiod AS ENUM (
  'WEEKLY',
  'MONTHLY',
  'QUARTERLY'
);

-- 미디어 역할 (variant_media 테이블에서 사용, v2.0 추가)
CREATE TYPE mediaroletype AS ENUM (
  'SOURCE',            -- 원본 미디어
  'EDITED',            -- 편집된 미디어
  'SUBTITLE',          -- 자막 파일
  'THUMBNAIL',         -- 썸네일 이미지
  'EFFECT'             -- 효과음/이모지
);
```

> **API 매핑 참고:** API의 `actionType` (예: `CONTENT_CREATE`)은 DB의 `audit_action` + `resource_type` 조합으로 표현.
> 예: `action=CREATE, resource_type='content'` → API 응답 시 `CONTENT_CREATE`로 직렬화.

---

## 3. Phase 1-A 테이블 (20개, v2.0: +content_variants, variant_media)

> **참고 (v1.3):** 실제 DB의 PK 기본값은 `uuid_generate_v4()` — pg_uuidv7 확장이 설치되지 않았으므로 v4를 사용.
> ENUM 타입명은 SQLAlchemy가 생성한 소문자 연결형 (예: `contentstatus`, `platformtype`)을 사용.

### 3.1 users — 사용자

> RLS 미적용. 여러 기관에 소속 가능 (다대다).

```sql
CREATE TABLE users (
  id              UUID PRIMARY KEY DEFAULT uuid_generate_v7(),
  email           VARCHAR(255) NOT NULL UNIQUE,
  password_hash   VARCHAR(255) NOT NULL,
  name            VARCHAR(100) NOT NULL,
  role            user_role NOT NULL DEFAULT 'AGENCY_OPERATOR',
  status          user_status NOT NULL DEFAULT 'ACTIVE',
  organization_id UUID REFERENCES organizations(id),  -- 기본 소속 기관 (nullable for SA)
  profile_image_url VARCHAR(512),
  preferences     JSONB NOT NULL DEFAULT '{}',         -- theme, language, defaultView
  last_login_at   TIMESTAMPTZ,
  failed_login_count INT NOT NULL DEFAULT 0,
  locked_until    TIMESTAMPTZ,
  created_at      TIMESTAMPTZ NOT NULL DEFAULT NOW(),
  updated_at      TIMESTAMPTZ NOT NULL DEFAULT NOW(),
  deleted_at      TIMESTAMPTZ                          -- soft delete
);

-- 인덱스
CREATE INDEX idx_users_email ON users(email);
CREATE INDEX idx_users_role ON users(role);
CREATE INDEX idx_users_status ON users(status);
CREATE INDEX idx_users_organization_id ON users(organization_id);
CREATE INDEX idx_users_deleted_at ON users(deleted_at) WHERE deleted_at IS NULL;
```

**컬럼 설명:**

| 컬럼 | 타입 | 설명 |
|---|---|---|
| id | UUID v7 | 시간 정렬 가능 PK |
| email | VARCHAR(255) | 로그인 ID, UNIQUE |
| password_hash | VARCHAR(255) | bcrypt 12 라운드 해시 |
| name | VARCHAR(100) | 표시 이름 |
| role | user_role | RBAC 역할 (SA/AM/AO/CD) |
| status | user_status | 계정 상태 |
| organization_id | UUID (nullable) | 기본 소속 기관. SA는 NULL 가능 |
| profile_image_url | VARCHAR(512) | 프로필 이미지 경로 (MinIO) |
| preferences | JSONB | `{ "theme": "light", "language": "ko", "defaultView": "list" }` |
| last_login_at | TIMESTAMPTZ | 최종 로그인 일시 |
| failed_login_count | INT | 연속 로그인 실패 횟수 (5회 → 잠금) |
| locked_until | TIMESTAMPTZ | 계정 잠금 해제 시각 |
| deleted_at | TIMESTAMPTZ | 소프트 삭제 일시 |

### 3.2 organizations — 위탁기관

> RLS 미적용. 기관 목록은 전역 조회 필요.

```sql
CREATE TABLE organizations (
  id                  UUID PRIMARY KEY DEFAULT uuid_generate_v7(),
  name                VARCHAR(200) NOT NULL,
  slug                VARCHAR(100) NOT NULL UNIQUE,
  status              org_status NOT NULL DEFAULT 'ACTIVE',
  plan                org_plan NOT NULL DEFAULT 'BASIC',
  logo_url            VARCHAR(512),
  contact_email       VARCHAR(255),
  contact_phone       VARCHAR(20),
  settings            JSONB NOT NULL DEFAULT '{}',
  storage_used_bytes  BIGINT NOT NULL DEFAULT 0,
  storage_quota_bytes BIGINT NOT NULL DEFAULT 53687091200,  -- 50GB
  created_at          TIMESTAMPTZ NOT NULL DEFAULT NOW(),
  updated_at          TIMESTAMPTZ NOT NULL DEFAULT NOW(),
  deleted_at          TIMESTAMPTZ
);

-- 인덱스
CREATE INDEX idx_organizations_slug ON organizations(slug);
CREATE INDEX idx_organizations_plan ON organizations(plan);
CREATE INDEX idx_organizations_settings ON organizations USING GIN(settings);
```

**`settings` JSONB 스키마:**

```json
{
  "defaultLanguage": "ko",
  "timezone": "Asia/Seoul",
  "features": {
    "aiEnabled": true,
    "aiMonthlyQuota": 1000,
    "aiUsedCount": 0,
    "telegramEnabled": false,
    "webPushEnabled": false
  },
  "approval": {
    "requiredCount": 1,
    "enableAiReview": false
  }
}
```

### 3.3 roles — 역할 정의

> RLS 미적용. 시스템 전역 역할.

```sql
CREATE TABLE roles (
  id          UUID PRIMARY KEY DEFAULT uuid_generate_v7(),
  name        VARCHAR(50) NOT NULL UNIQUE,
  permissions JSONB NOT NULL DEFAULT '[]',
  description VARCHAR(255),
  created_at  TIMESTAMPTZ NOT NULL DEFAULT NOW()
);
```

**`permissions` JSONB 스키마:**

```json
[
  "content.create", "content.edit", "content.delete", "content.publish",
  "comment.manage", "channel.manage", "user.manage", "report.*"
]
```

### 3.4 refresh_tokens — JWT 리프레시 토큰

> RLS 미적용. user_id 기반 조회.

```sql
CREATE TABLE refresh_tokens (
  id          UUID PRIMARY KEY DEFAULT uuid_generate_v7(),
  user_id     UUID NOT NULL REFERENCES users(id) ON DELETE CASCADE,
  token_hash  VARCHAR(255) NOT NULL UNIQUE,
  expires_at  TIMESTAMPTZ NOT NULL,
  is_revoked  BOOLEAN NOT NULL DEFAULT FALSE,
  device_info VARCHAR(500),
  created_at  TIMESTAMPTZ NOT NULL DEFAULT NOW()
);

-- 인덱스
CREATE INDEX idx_refresh_tokens_user_id ON refresh_tokens(user_id);
CREATE INDEX idx_refresh_tokens_expires_at ON refresh_tokens(expires_at);
CREATE INDEX idx_refresh_tokens_token_hash ON refresh_tokens(token_hash);
```

### 3.5 user_organizations — 사용자-기관 다대다 매핑

> RLS 미적용. 사용자의 다중 기관 소속 관리. AM/AO는 여러 기관 담당 가능.

```sql
CREATE TABLE user_organizations (
  id              UUID PRIMARY KEY DEFAULT uuid_generate_v7(),
  user_id         UUID NOT NULL REFERENCES users(id) ON DELETE CASCADE,
  organization_id UUID NOT NULL REFERENCES organizations(id) ON DELETE CASCADE,
  role            user_role NOT NULL,
  is_primary      BOOLEAN NOT NULL DEFAULT FALSE,       -- 기본 소속 기관
  created_at      TIMESTAMPTZ NOT NULL DEFAULT NOW(),

  CONSTRAINT uq_user_org UNIQUE(user_id, organization_id)
);

-- 인덱스
CREATE INDEX idx_user_organizations_user_id ON user_organizations(user_id);
CREATE INDEX idx_user_organizations_organization_id ON user_organizations(organization_id);
```

### 3.6 invitations — 사용자 초대

> RLS 미적용. 초대 토큰/상태/만료 관리.

```sql
CREATE TABLE invitations (
  id              UUID PRIMARY KEY DEFAULT uuid_generate_v7(),
  email           VARCHAR(255) NOT NULL,
  role            user_role NOT NULL,
  organization_id UUID NOT NULL REFERENCES organizations(id),
  token_hash      VARCHAR(255) NOT NULL UNIQUE,
  status          invitation_status NOT NULL DEFAULT 'PENDING',
  invited_by      UUID NOT NULL REFERENCES users(id),
  expires_at      TIMESTAMPTZ NOT NULL,
  accepted_at     TIMESTAMPTZ,
  created_at      TIMESTAMPTZ NOT NULL DEFAULT NOW()
);

-- 인덱스
CREATE INDEX idx_invitations_email ON invitations(email);
CREATE INDEX idx_invitations_token_hash ON invitations(token_hash);
CREATE INDEX idx_invitations_organization_id ON invitations(organization_id);
CREATE INDEX idx_invitations_status ON invitations(status);
```

### 3.7 password_reset_tokens — 비밀번호 재설정 토큰

> RLS 미적용. 비밀번호 재설정 흐름 지원.

```sql
CREATE TABLE password_reset_tokens (
  id          UUID PRIMARY KEY DEFAULT uuid_generate_v7(),
  user_id     UUID NOT NULL REFERENCES users(id) ON DELETE CASCADE,
  token_hash  VARCHAR(255) NOT NULL UNIQUE,
  expires_at  TIMESTAMPTZ NOT NULL,
  is_used     BOOLEAN NOT NULL DEFAULT FALSE,
  created_at  TIMESTAMPTZ NOT NULL DEFAULT NOW()
);

-- 인덱스
CREATE INDEX idx_password_reset_tokens_user_id ON password_reset_tokens(user_id);
CREATE INDEX idx_password_reset_tokens_token_hash ON password_reset_tokens(token_hash);
```

### 3.8 agencies — 수탁업체

> RLS 미적용. 수탁업체(조직) 관리. organizations와 1:N 관계.

```sql
CREATE TABLE agencies (
  id              UUID PRIMARY KEY DEFAULT uuid_generate_v7(),
  name            VARCHAR(200) NOT NULL,
  contact_email   VARCHAR(255),
  contact_phone   VARCHAR(20),
  is_active       BOOLEAN NOT NULL DEFAULT TRUE,
  created_at      TIMESTAMPTZ NOT NULL DEFAULT NOW(),
  updated_at      TIMESTAMPTZ NOT NULL DEFAULT NOW()
);

-- organizations에 agency_id FK 추가
ALTER TABLE organizations ADD COLUMN agency_id UUID REFERENCES agencies(id);
CREATE INDEX idx_organizations_agency_id ON organizations(agency_id);
```

### 3.9 system_announcements — 시스템 공지

> RLS 미적용. 시스템 전역 공지.

```sql
CREATE TABLE system_announcements (
  id          UUID PRIMARY KEY DEFAULT uuid_generate_v7(),
  title       VARCHAR(255) NOT NULL,
  content     TEXT NOT NULL,
  type        VARCHAR(50) NOT NULL DEFAULT 'INFO',     -- INFO, WARNING, MAINTENANCE
  is_active   BOOLEAN NOT NULL DEFAULT TRUE,
  publish_at  TIMESTAMPTZ,
  created_by  UUID REFERENCES users(id),
  created_at  TIMESTAMPTZ NOT NULL DEFAULT NOW(),
  updated_at  TIMESTAMPTZ NOT NULL DEFAULT NOW()
);
```

### 3.10 workspaces — 워크스페이스

> RLS 적용. 워크스페이스 전환 컨텍스트.

```sql
CREATE TABLE workspaces (
  id              UUID PRIMARY KEY DEFAULT uuid_generate_v7(),
  organization_id UUID NOT NULL REFERENCES organizations(id) ON DELETE CASCADE,
  name            VARCHAR(200) NOT NULL,
  is_default      BOOLEAN NOT NULL DEFAULT FALSE,
  created_at      TIMESTAMPTZ NOT NULL DEFAULT NOW()
);

-- 인덱스
CREATE INDEX idx_workspaces_organization_id ON workspaces(organization_id);

-- RLS
ALTER TABLE workspaces ENABLE ROW LEVEL SECURITY;
ALTER TABLE workspaces FORCE ROW LEVEL SECURITY;

CREATE POLICY tenant_isolation ON workspaces
  USING (organization_id = current_setting('app.current_org_id')::uuid);

CREATE POLICY sa_bypass ON workspaces
  USING (current_setting('app.user_role', true) = 'SYSTEM_ADMIN');
```

### 3.11 channels — 플랫폼 채널 연동

> RLS 적용. OAuth 토큰 Fernet 암호화 저장.

```sql
CREATE TABLE channels (
  id                      UUID PRIMARY KEY DEFAULT uuid_generate_v4(),
  organization_id         UUID NOT NULL REFERENCES organizations(id) ON DELETE CASCADE,
  platform                platformtype NOT NULL,
  platform_account_id     VARCHAR(255) NOT NULL,
  name                    VARCHAR(200) NOT NULL,
  status                  channelstatus NOT NULL DEFAULT 'DISCONNECTED',
  access_token_enc        TEXT,                           -- Fernet 암호화 (v1.2 BYTEA → TEXT)
  refresh_token_enc       TEXT,                           -- Fernet 암호화 (v1.2 BYTEA → TEXT)
  token_expires_at        TIMESTAMPTZ,
  metadata                JSONB,                          -- nullable (v1.2 NOT NULL DEFAULT '{}' 제거)
  last_comment_collected_at TIMESTAMPTZ,                  -- 댓글 수집 마지막 시각 (v1.3 추가)
  created_at              TIMESTAMPTZ NOT NULL DEFAULT NOW(),
  updated_at              TIMESTAMPTZ NOT NULL DEFAULT NOW(),

  CONSTRAINT uq_channels_org_platform_account
    UNIQUE(organization_id, platform, platform_account_id)
  -- v1.2 대비 제거된 컬럼: rate_limit_remaining, rate_limit_reset_at, connected_by, last_synced_at, deleted_at
);

-- 인덱스
CREATE INDEX idx_channels_organization_id ON channels(organization_id);
CREATE INDEX idx_channels_platform ON channels(platform);
CREATE INDEX idx_channels_status ON channels(status);
CREATE INDEX idx_channels_token_expires_at ON channels(token_expires_at);
CREATE INDEX idx_channels_metadata ON channels USING GIN(metadata);

-- RLS
ALTER TABLE channels ENABLE ROW LEVEL SECURITY;
ALTER TABLE channels FORCE ROW LEVEL SECURITY;

CREATE POLICY tenant_isolation ON channels
  USING (organization_id = current_setting('app.current_org_id')::uuid);

CREATE POLICY sa_bypass ON channels
  USING (current_setting('app.user_role', true) = 'SYSTEM_ADMIN');
```

**`metadata` JSONB 스키마:**

```json
{
  "followerCount": 15000,
  "profileUrl": "https://youtube.com/@channel",
  "profileImageUrl": "https://...",
  "capabilities": ["publish", "get_comments", "reply_comment", "get_analytics"]
}
```

### 3.12 channel_histories — 채널 연동 이력

> RLS 적용. 채널 연동/해제/갱신 이벤트 타임라인 추적.

```sql
CREATE TABLE channel_histories (
  id              UUID PRIMARY KEY DEFAULT uuid_generate_v7(),
  channel_id      UUID NOT NULL REFERENCES channels(id) ON DELETE CASCADE,
  organization_id UUID NOT NULL REFERENCES organizations(id),
  event_type      channel_event_type NOT NULL,
  details         JSONB NOT NULL DEFAULT '{}',     -- 이벤트 상세 (이전 상태, 오류 등)
  actor_id        UUID REFERENCES users(id),       -- 수동 작업 시 실행자
  created_at      TIMESTAMPTZ NOT NULL DEFAULT NOW()
);

-- 인덱스
CREATE INDEX idx_channel_histories_channel_id ON channel_histories(channel_id);
CREATE INDEX idx_channel_histories_organization_id ON channel_histories(organization_id);
CREATE INDEX idx_channel_histories_event_type ON channel_histories(event_type);
CREATE INDEX idx_channel_histories_created_at ON channel_histories(created_at DESC);

-- 복합 인덱스 (채널별 이력 타임라인)
CREATE INDEX idx_channel_histories_channel_timeline
  ON channel_histories(channel_id, created_at DESC);

-- RLS
ALTER TABLE channel_histories ENABLE ROW LEVEL SECURITY;
ALTER TABLE channel_histories FORCE ROW LEVEL SECURITY;

CREATE POLICY tenant_isolation ON channel_histories
  USING (organization_id = current_setting('app.current_org_id')::uuid);

CREATE POLICY sa_bypass ON channel_histories
  USING (current_setting('app.user_role', true) = 'SYSTEM_ADMIN');
```

**`details` JSONB 스키마 예시:**

```json
{
  "previousStatus": "ACTIVE",
  "newStatus": "TOKEN_EXPIRED",
  "reason": "OAuth refresh token expired",
  "tokenExpiresAt": "2026-03-01T00:00:00Z"
}
```

### 3.13 contents — 콘텐츠 게시물

> RLS 적용. 멀티플랫폼 게시 중심 테이블.

```sql
CREATE TABLE contents (
  id              UUID PRIMARY KEY DEFAULT uuid_generate_v4(),
  organization_id UUID NOT NULL REFERENCES organizations(id) ON DELETE CASCADE,
  title           VARCHAR(255) NOT NULL,
  body            TEXT,
  status          contentstatus NOT NULL DEFAULT 'DRAFT',
  platforms       VARCHAR(50)[] DEFAULT '{}',             -- 대상 플랫폼 배열 (VARCHAR[], v1.2 platform_type[] → 변경)
  channel_ids     UUID[] DEFAULT '{}',                    -- 대상 채널 ID 배열
  scheduled_at    TIMESTAMPTZ,
  author_id       UUID NOT NULL REFERENCES users(id),
  platform_contents JSONB,                                -- 플랫폼별 콘텐츠 (nullable)
  metadata        JSONB,                                  -- 메타데이터 (nullable)
  ai_generated    BOOLEAN NOT NULL DEFAULT FALSE,         -- AI 생성 여부 플래그 (v1.2 JSONB → BOOLEAN 변경)
  media_urls      VARCHAR(1024)[] DEFAULT '{}',           -- 미디어 URL 배열 (v1.3 추가)
  source_media_id UUID REFERENCES media_assets(id),       -- 원본 미디어 FK (v2.0 추가)
  search_vector   tsvector,                               -- 전문 검색
  deleted_at      TIMESTAMPTZ,
  created_at      TIMESTAMPTZ NOT NULL DEFAULT NOW(),
  updated_at      TIMESTAMPTZ NOT NULL DEFAULT NOW()
  -- v1.2 대비 제거된 컬럼: published_at, is_urgent, memo, version
);

-- 인덱스
CREATE INDEX idx_contents_organization_id ON contents(organization_id);
CREATE INDEX idx_contents_status ON contents(status);
CREATE INDEX idx_contents_author_id ON contents(author_id);
CREATE INDEX idx_contents_scheduled_at ON contents(scheduled_at);
CREATE INDEX idx_contents_published_at ON contents(published_at);
CREATE INDEX idx_contents_platforms ON contents USING GIN(platforms);
CREATE INDEX idx_contents_channel_ids ON contents USING GIN(channel_ids);
CREATE INDEX idx_contents_metadata ON contents USING GIN(metadata);
CREATE INDEX idx_contents_ai_generated ON contents USING GIN(ai_generated);
CREATE INDEX idx_contents_search_vector ON contents USING GIN(search_vector);

-- 복합 인덱스 (콘텐츠 목록 필터 + 정렬)
CREATE INDEX idx_contents_org_status_created
  ON contents(organization_id, status, created_at DESC);

-- 예약 게시 스캔용 부분 인덱스
CREATE INDEX idx_contents_scheduled_pending
  ON contents(scheduled_at)
  WHERE status = 'SCHEDULED' AND deleted_at IS NULL;

-- tsvector 자동 갱신 트리거
CREATE OR REPLACE FUNCTION contents_search_vector_update() RETURNS TRIGGER AS $$
BEGIN
  NEW.search_vector :=
    setweight(to_tsvector('simple', COALESCE(NEW.title, '')), 'A') ||
    setweight(to_tsvector('simple', COALESCE(NEW.body, '')), 'B');
  RETURN NEW;
END;
$$ LANGUAGE plpgsql;

CREATE TRIGGER trg_contents_search_vector
  BEFORE INSERT OR UPDATE OF title, body ON contents
  FOR EACH ROW
  EXECUTE FUNCTION contents_search_vector_update();

-- RLS
ALTER TABLE contents ENABLE ROW LEVEL SECURITY;
ALTER TABLE contents FORCE ROW LEVEL SECURITY;

CREATE POLICY tenant_isolation ON contents
  USING (organization_id = current_setting('app.current_org_id')::uuid);

CREATE POLICY sa_bypass ON contents
  USING (current_setting('app.user_role', true) = 'SYSTEM_ADMIN');
```

**`platform_contents` JSONB 스키마:**

```json
{
  "youtube": { "title": "...", "description": "...", "tags": ["tag1"] },
  "instagram": { "caption": "...", "altText": "..." },
  "facebook": { "message": "..." },
  "x": { "text": "..." },
  "naver_blog": { "title": "...", "content": "..." }
}
```

**`metadata` JSONB 스키마:**

```json
{
  "hashtags": ["#공공기관", "#서울시"],
  "thumbnailUrl": "https://minio.../thumb.jpg",
  "ogImage": "https://...",
  "scheduledChannels": [
    { "channelId": "uuid", "platform": "YOUTUBE", "scheduledAt": "2026-03-05T10:00:00Z" }
  ],
  "mediaIds": ["uuid1", "uuid2"]
}
```

**`ai_generated` JSONB 스키마:**

```json
{
  "hashtags": {
    "result": ["#추천", "#공공"],
    "model": "gpt-4o-mini",
    "confidence": 0.87,
    "usedAt": "2026-03-04T10:00:00Z"
  },
  "metaDescription": {
    "result": "서울시 공공기관...",
    "model": "gpt-4o-mini",
    "confidence": 0.92,
    "usedAt": "2026-03-04T10:01:00Z"
  },
  "title": {
    "result": "AI 생성 제목",
    "model": "gpt-4o-mini",
    "confidence": 0.85,
    "usedAt": "2026-03-04T10:02:00Z"
  },
  "review": {
    "expression": { "status": "PASS", "message": "...", "details": [] },
    "privacy": { "status": "WARNING", "message": "...", "details": [] },
    "accessibility": { "status": "PASS", "message": "...", "details": [] },
    "copyright": { "status": "PASS", "message": "...", "details": [] },
    "overallStatus": "WARNING",
    "model": "gpt-4o",
    "usedAt": "2026-03-04T10:03:00Z"
  }
}
```

### 3.14 content_variants — 플랫폼별 파생본 (v2.0 추가)

> RLS 적용. 콘텐츠의 플랫폼별 커스터마이즈 파생본 관리.

```sql
CREATE TABLE content_variants (
  id              UUID PRIMARY KEY DEFAULT uuid_generate_v4(),
  content_id      UUID NOT NULL REFERENCES contents(id) ON DELETE CASCADE,
  organization_id UUID NOT NULL REFERENCES organizations(id),
  platform        platformtype NOT NULL,                  -- 대상 플랫폼
  channel_id      UUID REFERENCES channels(id),           -- 대상 채널 (nullable, 플랫폼별 기본)
  title           VARCHAR(255),                            -- 제목 오버라이드 (null이면 contents.title 사용)
  body            TEXT,                                    -- 본문 오버라이드 (null이면 contents.body 사용)
  metadata        JSONB,                                   -- 플랫폼별 메타 (해시태그, 카테고리 등)
  created_at      TIMESTAMPTZ NOT NULL DEFAULT NOW(),
  updated_at      TIMESTAMPTZ NOT NULL DEFAULT NOW()
);

-- 인덱스
CREATE INDEX idx_content_variants_content_id ON content_variants(content_id);
CREATE INDEX idx_content_variants_organization_id ON content_variants(organization_id);
CREATE INDEX idx_content_variants_platform ON content_variants(platform);
CREATE INDEX idx_content_variants_channel_id ON content_variants(channel_id);

-- 유니크 제약 (콘텐츠당 플랫폼+채널 조합 유일)
CREATE UNIQUE INDEX idx_content_variants_unique
  ON content_variants(content_id, platform, COALESCE(channel_id, '00000000-0000-0000-0000-000000000000'::uuid));

-- RLS
ALTER TABLE content_variants ENABLE ROW LEVEL SECURITY;
ALTER TABLE content_variants FORCE ROW LEVEL SECURITY;

CREATE POLICY tenant_isolation ON content_variants
  USING (organization_id = current_setting('app.current_org_id')::uuid);

CREATE POLICY sa_bypass ON content_variants
  USING (current_setting('app.user_role', true) = 'SYSTEM_ADMIN');
```

### 3.14a variant_media — 파생본-미디어 관계 (v2.0 추가)

> RLS 적용. content_variants와 media_assets 간 다대다 관계 + 역할 구분.

```sql
CREATE TABLE variant_media (
  id              UUID PRIMARY KEY DEFAULT uuid_generate_v4(),
  variant_id      UUID NOT NULL REFERENCES content_variants(id) ON DELETE CASCADE,
  media_id        UUID NOT NULL REFERENCES media_assets(id) ON DELETE CASCADE,
  organization_id UUID NOT NULL REFERENCES organizations(id),
  role            mediaroletype NOT NULL DEFAULT 'SOURCE', -- 미디어 역할
  sort_order      INT NOT NULL DEFAULT 0,                  -- 정렬 순서
  created_at      TIMESTAMPTZ NOT NULL DEFAULT NOW()
);

-- 인덱스
CREATE INDEX idx_variant_media_variant_id ON variant_media(variant_id);
CREATE INDEX idx_variant_media_media_id ON variant_media(media_id);
CREATE INDEX idx_variant_media_organization_id ON variant_media(organization_id);
CREATE INDEX idx_variant_media_role ON variant_media(role);

-- 유니크 제약 (variant당 media+role 조합 유일)
CREATE UNIQUE INDEX idx_variant_media_unique
  ON variant_media(variant_id, media_id, role);

-- RLS
ALTER TABLE variant_media ENABLE ROW LEVEL SECURITY;
ALTER TABLE variant_media FORCE ROW LEVEL SECURITY;

CREATE POLICY tenant_isolation ON variant_media
  USING (organization_id = current_setting('app.current_org_id')::uuid);

CREATE POLICY sa_bypass ON variant_media
  USING (current_setting('app.user_role', true) = 'SYSTEM_ADMIN');
```

### 3.15 content_versions — 콘텐츠 버전 이력

> RLS 적용. 콘텐츠 수정 시 이전 버전 보존.

```sql
CREATE TABLE content_versions (
  id              UUID PRIMARY KEY DEFAULT uuid_generate_v7(),
  content_id      UUID NOT NULL REFERENCES contents(id) ON DELETE CASCADE,
  organization_id UUID NOT NULL REFERENCES organizations(id),
  version         INT NOT NULL,
  title           VARCHAR(255) NOT NULL,
  body            TEXT,
  platform_contents JSONB NOT NULL DEFAULT '{}',
  metadata        JSONB NOT NULL DEFAULT '{}',
  changed_by      UUID NOT NULL REFERENCES users(id),
  created_at      TIMESTAMPTZ NOT NULL DEFAULT NOW(),

  CONSTRAINT uq_content_versions_content_version
    UNIQUE(content_id, version)
);

-- 인덱스
CREATE INDEX idx_content_versions_content_id ON content_versions(content_id);
CREATE INDEX idx_content_versions_organization_id ON content_versions(organization_id);

-- RLS
ALTER TABLE content_versions ENABLE ROW LEVEL SECURITY;
ALTER TABLE content_versions FORCE ROW LEVEL SECURITY;

CREATE POLICY tenant_isolation ON content_versions
  USING (organization_id = current_setting('app.current_org_id')::uuid);

CREATE POLICY sa_bypass ON content_versions
  USING (current_setting('app.user_role', true) = 'SYSTEM_ADMIN');
```

### 3.15 publish_results — 플랫폼별 게시 결과

> RLS 적용. 채널별 게시 상태 추적.

```sql
CREATE TABLE publish_results (
  id               UUID PRIMARY KEY DEFAULT uuid_generate_v4(),
  content_id       UUID NOT NULL REFERENCES contents(id) ON DELETE CASCADE,
  variant_id       UUID REFERENCES content_variants(id),   -- v2.0 추가: 파생본 기반 게시 시 참조
  organization_id  UUID NOT NULL REFERENCES organizations(id),
  channel_id       UUID NOT NULL REFERENCES channels(id),
  status           publishresultstatus NOT NULL DEFAULT 'PENDING',
  platform_post_id VARCHAR(255),               -- 플랫폼 고유 게시물 ID
  platform_url     VARCHAR(1024),              -- 플랫폼 게시물 URL
  error_message    TEXT,
  retry_count      INT NOT NULL DEFAULT 0,
  views            INT NOT NULL DEFAULT 0,             -- 조회수 (플랫폼 동기화)
  likes            INT NOT NULL DEFAULT 0,             -- 좋아요 수
  shares           INT NOT NULL DEFAULT 0,             -- 공유 수
  comments_count   INT NOT NULL DEFAULT 0,             -- 댓글 수
  created_at       TIMESTAMPTZ NOT NULL DEFAULT NOW(),
  updated_at       TIMESTAMPTZ NOT NULL DEFAULT NOW()
  -- v1.2 대비 제거된 컬럼: error_code, max_retries, metrics_fetched_at, published_at
);

-- 인덱스
CREATE INDEX idx_publish_results_content_id ON publish_results(content_id);
CREATE INDEX idx_publish_results_organization_id ON publish_results(organization_id);
CREATE INDEX idx_publish_results_channel_id ON publish_results(channel_id);
CREATE INDEX idx_publish_results_status ON publish_results(status);

-- RLS
ALTER TABLE publish_results ENABLE ROW LEVEL SECURITY;
ALTER TABLE publish_results FORCE ROW LEVEL SECURITY;

CREATE POLICY tenant_isolation ON publish_results
  USING (organization_id = current_setting('app.current_org_id')::uuid);

CREATE POLICY sa_bypass ON publish_results
  USING (current_setting('app.user_role', true) = 'SYSTEM_ADMIN');
```

### 3.16 approval_workflows — 승인 워크플로우 설정

> RLS 적용. 기관별 승인 절차 구성.

```sql
CREATE TABLE approval_workflows (
  id              UUID PRIMARY KEY DEFAULT uuid_generate_v7(),
  organization_id UUID NOT NULL REFERENCES organizations(id) ON DELETE CASCADE,
  name            VARCHAR(200) NOT NULL,
  steps           JSONB NOT NULL DEFAULT '[]',
  is_active       BOOLEAN NOT NULL DEFAULT TRUE,
  created_at      TIMESTAMPTZ NOT NULL DEFAULT NOW(),
  updated_at      TIMESTAMPTZ NOT NULL DEFAULT NOW()
);

-- 인덱스
CREATE INDEX idx_approval_workflows_organization_id ON approval_workflows(organization_id);

-- RLS
ALTER TABLE approval_workflows ENABLE ROW LEVEL SECURITY;
ALTER TABLE approval_workflows FORCE ROW LEVEL SECURITY;

CREATE POLICY tenant_isolation ON approval_workflows
  USING (organization_id = current_setting('app.current_org_id')::uuid);

CREATE POLICY sa_bypass ON approval_workflows
  USING (current_setting('app.user_role', true) = 'SYSTEM_ADMIN');
```

**`steps` JSONB 스키마:**

```json
{
  "steps": [
    {
      "step": 1,
      "approverRole": "CLIENT_DIRECTOR",
      "approverIds": ["uuid1", "uuid2"],
      "requiredCount": 1
    }
  ],
  "fastTrack": {
    "enabled": true,
    "allowedRoles": ["AGENCY_MANAGER"],
    "postPublishReviewRequired": true
  },
  "autoRejectConditions": {
    "maxPendingDays": 7,
    "enabled": false
  }
}
```

### 3.17 approval_requests — 승인 요청

> RLS 적용. 콘텐츠별 승인 프로세스 추적.

```sql
CREATE TABLE approval_requests (
  id              UUID PRIMARY KEY DEFAULT uuid_generate_v4(),
  content_id      UUID NOT NULL REFERENCES contents(id) ON DELETE CASCADE,
  organization_id UUID NOT NULL REFERENCES organizations(id),
  workflow_id     UUID REFERENCES approval_workflows(id),
  current_step    INT NOT NULL DEFAULT 1,
  status          approvalstatus NOT NULL DEFAULT 'PENDING_REVIEW',  -- 전용 ENUM (v1.2 content_status → 변경)
  requested_by    UUID NOT NULL REFERENCES users(id),
  is_urgent       BOOLEAN NOT NULL DEFAULT FALSE,                    -- 긴급 여부 (v1.3 추가)
  comment         TEXT,                                              -- 요청 코멘트 (v1.3 추가)
  created_at      TIMESTAMPTZ NOT NULL DEFAULT NOW(),
  updated_at      TIMESTAMPTZ NOT NULL DEFAULT NOW()
);

-- 인덱스
CREATE INDEX idx_approval_requests_content_id ON approval_requests(content_id);
CREATE INDEX idx_approval_requests_organization_id ON approval_requests(organization_id);
CREATE INDEX idx_approval_requests_status ON approval_requests(status);
CREATE INDEX idx_approval_requests_requested_by ON approval_requests(requested_by);

-- 복합 인덱스 (승인 대기 목록)
CREATE INDEX idx_approval_requests_org_status_created
  ON approval_requests(organization_id, status, created_at DESC);

-- RLS
ALTER TABLE approval_requests ENABLE ROW LEVEL SECURITY;
ALTER TABLE approval_requests FORCE ROW LEVEL SECURITY;

CREATE POLICY tenant_isolation ON approval_requests
  USING (organization_id = current_setting('app.current_org_id')::uuid);

CREATE POLICY sa_bypass ON approval_requests
  USING (current_setting('app.user_role', true) = 'SYSTEM_ADMIN');
```

### 3.18 approval_histories — 승인 이력

> RLS 적용. 승인/반려 이력 추적.

```sql
CREATE TABLE approval_histories (
  id              UUID PRIMARY KEY DEFAULT uuid_generate_v4(),
  request_id      UUID NOT NULL REFERENCES approval_requests(id) ON DELETE CASCADE,
  organization_id UUID NOT NULL REFERENCES organizations(id),
  step            INT NOT NULL,
  action          approvalaction NOT NULL,
  reviewer_id     UUID REFERENCES users(id),             -- nullable (v1.2 NOT NULL → nullable)
  variant_id      UUID REFERENCES content_variants(id),  -- v2.0 추가: 파생본별 피드백 시 참조
  comment         TEXT,
  created_at      TIMESTAMPTZ NOT NULL DEFAULT NOW(),
  updated_at      TIMESTAMPTZ NOT NULL DEFAULT NOW()     -- v1.3 추가
);

-- 인덱스
CREATE INDEX idx_approval_histories_request_id ON approval_histories(request_id);
CREATE INDEX idx_approval_histories_organization_id ON approval_histories(organization_id);
CREATE INDEX idx_approval_histories_reviewer_id ON approval_histories(reviewer_id);

-- RLS
ALTER TABLE approval_histories ENABLE ROW LEVEL SECURITY;
ALTER TABLE approval_histories FORCE ROW LEVEL SECURITY;

CREATE POLICY tenant_isolation ON approval_histories
  USING (organization_id = current_setting('app.current_org_id')::uuid);

CREATE POLICY sa_bypass ON approval_histories
  USING (current_setting('app.user_role', true) = 'SYSTEM_ADMIN');
```

---

## 4. Phase 1-B 테이블 (8개)

### 4.1 comments — 댓글 수집/관리

> RLS 적용. 5개 플랫폼 댓글 통합 관리.

```sql
CREATE TABLE comments (
  id                UUID PRIMARY KEY DEFAULT uuid_generate_v7(),
  organization_id   UUID NOT NULL REFERENCES organizations(id),
  content_id        UUID REFERENCES contents(id),
  channel_id        UUID NOT NULL REFERENCES channels(id),
  platform          platform_type NOT NULL,
  external_id       VARCHAR(255) NOT NULL,              -- 플랫폼 댓글 ID
  text              TEXT NOT NULL,
  author_name       VARCHAR(200),
  author_profile_url VARCHAR(512),
  parent_comment_id UUID REFERENCES comments(id),       -- 부모 댓글 (스레드)
  sentiment         comment_sentiment,                  -- AI 분석 결과 (nullable)
  sentiment_confidence FLOAT,                           -- 감성 분석 신뢰도
  dangerous_level   VARCHAR(20),                        -- low, medium, high
  keywords          VARCHAR(100)[] DEFAULT '{}',        -- AI 추출 키워드
  status            comment_status NOT NULL DEFAULT 'UNPROCESSED',
  reply_text        TEXT,                               -- 답글 내용 (게시된 답글)
  reply_draft       TEXT,                               -- AI 답글 초안 (임시)
  replied_at        TIMESTAMPTZ,
  hidden_reason     TEXT,
  delete_reason     TEXT,
  processed_by      UUID REFERENCES users(id),
  platform_created_at TIMESTAMPTZ,                      -- 플랫폼 원본 작성 시각
  search_vector     tsvector,                           -- 댓글 전문 검색
  created_at        TIMESTAMPTZ NOT NULL DEFAULT NOW(),
  updated_at        TIMESTAMPTZ NOT NULL DEFAULT NOW(),

  CONSTRAINT uq_comments_platform_external
    UNIQUE(channel_id, external_id)
);

-- 인덱스
CREATE INDEX idx_comments_organization_id ON comments(organization_id);
CREATE INDEX idx_comments_content_id ON comments(content_id);
CREATE INDEX idx_comments_channel_id ON comments(channel_id);
CREATE INDEX idx_comments_platform ON comments(platform);
CREATE INDEX idx_comments_sentiment ON comments(sentiment);
CREATE INDEX idx_comments_created_at ON comments(created_at DESC);

CREATE INDEX idx_comments_status ON comments(status);
CREATE INDEX idx_comments_parent_comment_id ON comments(parent_comment_id);
CREATE INDEX idx_comments_keywords ON comments USING GIN(keywords);
CREATE INDEX idx_comments_search_vector ON comments USING GIN(search_vector);

-- 복합 인덱스 (댓글 목록 필터)
CREATE INDEX idx_comments_org_platform_sentiment
  ON comments(organization_id, platform, sentiment);

-- 위험 댓글 부분 인덱스
CREATE INDEX idx_comments_dangerous
  ON comments(organization_id, created_at DESC)
  WHERE sentiment = 'DANGEROUS';

-- tsvector 자동 갱신 트리거
CREATE TRIGGER trg_comments_search_vector
  BEFORE INSERT OR UPDATE OF text ON comments
  FOR EACH ROW
  EXECUTE FUNCTION (
    NEW.search_vector := to_tsvector('simple', COALESCE(NEW.text, ''))
  );

-- RLS
ALTER TABLE comments ENABLE ROW LEVEL SECURITY;
ALTER TABLE comments FORCE ROW LEVEL SECURITY;

CREATE POLICY tenant_isolation ON comments
  USING (organization_id = current_setting('app.current_org_id')::uuid);

CREATE POLICY sa_bypass ON comments
  USING (current_setting('app.user_role', true) = 'SYSTEM_ADMIN');
```

### 4.2 notifications — 알림

> RLS 적용. 사용자별 알림.

```sql
CREATE TABLE notifications (
  id              UUID PRIMARY KEY DEFAULT uuid_generate_v7(),
  organization_id UUID NOT NULL REFERENCES organizations(id),
  user_id         UUID NOT NULL REFERENCES users(id),
  type            notification_type NOT NULL,
  channel         notification_channel NOT NULL DEFAULT 'IN_APP',
  title           VARCHAR(255) NOT NULL,
  message         TEXT NOT NULL,
  payload         JSONB NOT NULL DEFAULT '{}',
  is_read         BOOLEAN NOT NULL DEFAULT FALSE,
  read_at         TIMESTAMPTZ,
  action_url      VARCHAR(1024),
  created_at      TIMESTAMPTZ NOT NULL DEFAULT NOW()
);

-- 인덱스
CREATE INDEX idx_notifications_organization_id ON notifications(organization_id);
CREATE INDEX idx_notifications_user_id ON notifications(user_id);
CREATE INDEX idx_notifications_is_read ON notifications(is_read);
CREATE INDEX idx_notifications_type ON notifications(type);
CREATE INDEX idx_notifications_created_at ON notifications(created_at DESC);

-- 복합 인덱스 (미읽음 알림)
CREATE INDEX idx_notifications_user_unread
  ON notifications(user_id, created_at DESC)
  WHERE is_read = FALSE;

-- RLS
ALTER TABLE notifications ENABLE ROW LEVEL SECURITY;
ALTER TABLE notifications FORCE ROW LEVEL SECURITY;

CREATE POLICY tenant_isolation ON notifications
  USING (organization_id = current_setting('app.current_org_id')::uuid);

CREATE POLICY sa_bypass ON notifications
  USING (current_setting('app.user_role', true) = 'SYSTEM_ADMIN');
```

**`payload` JSONB 스키마:**

```json
{
  "contentId": "uuid",
  "channelId": "uuid",
  "commentId": "uuid",
  "message": "위험 댓글이 감지되었습니다.",
  "actionUrl": "/contents/uuid/comments"
}
```

### 4.3 notification_settings — 사용자별 알림 설정

> RLS 적용. 알림 채널별 on/off, 텔레그램/웹 푸시 구독 정보.

```sql
CREATE TABLE notification_settings (
  id                UUID PRIMARY KEY DEFAULT uuid_generate_v7(),
  organization_id   UUID NOT NULL REFERENCES organizations(id),
  user_id           UUID NOT NULL REFERENCES users(id),
  channels          JSONB NOT NULL DEFAULT '{}',
  push_subscription JSONB,                          -- Web Push 구독 정보
  telegram_chat_id  VARCHAR(100),                   -- 텔레그램 Chat ID
  created_at        TIMESTAMPTZ NOT NULL DEFAULT NOW(),
  updated_at        TIMESTAMPTZ NOT NULL DEFAULT NOW(),

  CONSTRAINT uq_notification_settings_org_user
    UNIQUE(organization_id, user_id)
);

-- 인덱스
CREATE INDEX idx_notification_settings_user_id ON notification_settings(user_id);
CREATE INDEX idx_notification_settings_organization_id ON notification_settings(organization_id);

-- RLS
ALTER TABLE notification_settings ENABLE ROW LEVEL SECURITY;
ALTER TABLE notification_settings FORCE ROW LEVEL SECURITY;

CREATE POLICY tenant_isolation ON notification_settings
  USING (organization_id = current_setting('app.current_org_id')::uuid);

CREATE POLICY sa_bypass ON notification_settings
  USING (current_setting('app.user_role', true) = 'SYSTEM_ADMIN');
```

**`channels` JSONB 스키마:**

```json
{
  "web": { "enabled": true },
  "email": { "enabled": true },
  "telegram": {
    "enabled": false,
    "botToken": "...",
    "channels": [
      { "id": "ch1", "name": "알림채널", "chatId": "-100123456" }
    ]
  },
  "webPush": { "enabled": false }
}
```

### 4.4 reply_templates — 답글 템플릿

> RLS 적용. 댓글 답글 템플릿 관리. (v1.2 `templates` → `reply_templates`로 테이블명 변경)

```sql
CREATE TABLE reply_templates (
  id              UUID PRIMARY KEY DEFAULT uuid_generate_v4(),
  organization_id UUID NOT NULL REFERENCES organizations(id),
  category        VARCHAR(100) NOT NULL,
  name            VARCHAR(200) NOT NULL,
  content         TEXT NOT NULL,
  variables       VARCHAR(100)[] NOT NULL DEFAULT '{}',  -- {기관명}, {담당자} 등
  usage_count     INT NOT NULL DEFAULT 0,
  is_active       BOOLEAN NOT NULL DEFAULT TRUE,
  created_by      UUID NOT NULL REFERENCES users(id),
  created_at      TIMESTAMPTZ NOT NULL DEFAULT NOW(),
  updated_at      TIMESTAMPTZ NOT NULL DEFAULT NOW(),
  deleted_at      TIMESTAMPTZ
);

-- 인덱스
CREATE INDEX idx_templates_organization_id ON templates(organization_id);
CREATE INDEX idx_templates_category ON templates(category);
CREATE INDEX idx_templates_is_active ON templates(is_active);

-- RLS
ALTER TABLE templates ENABLE ROW LEVEL SECURITY;
ALTER TABLE templates FORCE ROW LEVEL SECURITY;

CREATE POLICY tenant_isolation ON templates
  USING (organization_id = current_setting('app.current_org_id')::uuid);

CREATE POLICY sa_bypass ON templates
  USING (current_setting('app.user_role', true) = 'SYSTEM_ADMIN');
```

### 4.5 audit_logs — 감사 로그

> RLS 적용. INSERT-ONLY (UPDATE/DELETE 차단). 월별 파티셔닝.

```sql
CREATE TABLE audit_logs (
  id              UUID NOT NULL DEFAULT uuid_generate_v7(),
  organization_id UUID NOT NULL REFERENCES organizations(id),
  actor_id        UUID REFERENCES users(id),
  actor_role      user_role,
  action          audit_action NOT NULL,
  resource_type   VARCHAR(50) NOT NULL,                -- content, comment, channel, user, approval 등
  resource_id     UUID,
  changes         JSONB NOT NULL DEFAULT '{}',         -- before/after (PII 마스킹)
  ip_address      VARCHAR(45),                         -- IPv4/IPv6
  user_agent      VARCHAR(500),
  request_id      UUID,                                -- structlog 상관 ID
  created_at      TIMESTAMPTZ NOT NULL DEFAULT NOW(),

  PRIMARY KEY (id, created_at)                         -- 파티션 키 포함
) PARTITION BY RANGE (created_at);

-- 기본 파티션 (초기)
CREATE TABLE audit_logs_2026_03 PARTITION OF audit_logs
  FOR VALUES FROM ('2026-03-01') TO ('2026-04-01');

CREATE TABLE audit_logs_2026_04 PARTITION OF audit_logs
  FOR VALUES FROM ('2026-04-01') TO ('2026-05-01');

-- 인덱스 (각 파티션에 자동 생성)
CREATE INDEX idx_audit_logs_organization_id ON audit_logs(organization_id);
CREATE INDEX idx_audit_logs_actor_id ON audit_logs(actor_id);
CREATE INDEX idx_audit_logs_action ON audit_logs(action);
CREATE INDEX idx_audit_logs_resource_type ON audit_logs(resource_type);
CREATE INDEX idx_audit_logs_created_at ON audit_logs(created_at DESC);

-- 복합 인덱스
CREATE INDEX idx_audit_logs_org_action_created
  ON audit_logs(organization_id, action, created_at DESC);

-- INSERT-ONLY 트리거 (UPDATE/DELETE 차단)
CREATE OR REPLACE FUNCTION prevent_audit_modification() RETURNS TRIGGER AS $$
BEGIN
  RAISE EXCEPTION 'audit_logs table is INSERT-ONLY. UPDATE and DELETE are prohibited.';
  RETURN NULL;
END;
$$ LANGUAGE plpgsql;

CREATE TRIGGER trg_prevent_audit_update
  BEFORE UPDATE ON audit_logs
  FOR EACH ROW
  EXECUTE FUNCTION prevent_audit_modification();

CREATE TRIGGER trg_prevent_audit_delete
  BEFORE DELETE ON audit_logs
  FOR EACH ROW
  EXECUTE FUNCTION prevent_audit_modification();

-- RLS
ALTER TABLE audit_logs ENABLE ROW LEVEL SECURITY;
ALTER TABLE audit_logs FORCE ROW LEVEL SECURITY;

CREATE POLICY tenant_isolation ON audit_logs
  USING (organization_id = current_setting('app.current_org_id')::uuid);

CREATE POLICY sa_bypass ON audit_logs
  USING (current_setting('app.user_role', true) = 'SYSTEM_ADMIN');
```

**`changes` JSONB 스키마:**

```json
{
  "title": {
    "before": "이전 제목",
    "after": "수정된 제목"
  },
  "status": {
    "before": "DRAFT",
    "after": "PENDING_APPROVAL"
  }
}
```

### 4.6 ai_usage_logs — AI 사용 이력/비용 추적

> RLS 적용. AI 호출별 비용·토큰 추적.

```sql
CREATE TABLE ai_usage_logs (
  id              UUID PRIMARY KEY DEFAULT uuid_generate_v7(),
  organization_id UUID NOT NULL REFERENCES organizations(id),
  user_id         UUID REFERENCES users(id),
  task_type       ai_task_type NOT NULL,
  model           VARCHAR(100) NOT NULL,               -- gpt-4o-mini, claude-sonnet 등
  prompt_tokens   INT NOT NULL DEFAULT 0,
  completion_tokens INT NOT NULL DEFAULT 0,
  total_tokens    INT NOT NULL DEFAULT 0,
  estimated_cost  DECIMAL(10, 6) NOT NULL DEFAULT 0,   -- USD
  processing_time_ms INT,
  input_summary   VARCHAR(500),                        -- 입력 요약 (PII 제외)
  output_summary  VARCHAR(500),                        -- 출력 요약
  is_fallback     BOOLEAN NOT NULL DEFAULT FALSE,
  error_message   TEXT,
  created_at      TIMESTAMPTZ NOT NULL DEFAULT NOW()
);

-- 인덱스
CREATE INDEX idx_ai_usage_logs_organization_id ON ai_usage_logs(organization_id);
CREATE INDEX idx_ai_usage_logs_user_id ON ai_usage_logs(user_id);
CREATE INDEX idx_ai_usage_logs_task_type ON ai_usage_logs(task_type);
CREATE INDEX idx_ai_usage_logs_model ON ai_usage_logs(model);
CREATE INDEX idx_ai_usage_logs_created_at ON ai_usage_logs(created_at DESC);

-- 월별 비용 집계용 복합 인덱스
CREATE INDEX idx_ai_usage_logs_org_month
  ON ai_usage_logs(organization_id, date_trunc('month', created_at));

-- RLS
ALTER TABLE ai_usage_logs ENABLE ROW LEVEL SECURITY;
ALTER TABLE ai_usage_logs FORCE ROW LEVEL SECURITY;

CREATE POLICY tenant_isolation ON ai_usage_logs
  USING (organization_id = current_setting('app.current_org_id')::uuid);

CREATE POLICY sa_bypass ON ai_usage_logs
  USING (current_setting('app.user_role', true) = 'SYSTEM_ADMIN');
```

### 4.7 ai_jobs — AI 비동기 작업 (v1.3 추가)

> RLS 적용. 자막 생성, 숏폼 추출 등 장시간 AI 작업의 진행 상태 추적.

```sql
CREATE TABLE ai_jobs (
  id              UUID PRIMARY KEY DEFAULT uuid_generate_v4(),
  organization_id UUID NOT NULL REFERENCES organizations(id),
  user_id         UUID NOT NULL REFERENCES users(id),
  job_type        aijobtype NOT NULL,                    -- SUBTITLE, SHORTFORM, REPORT
  status          aijobstatus NOT NULL DEFAULT 'PENDING',
  progress        INT NOT NULL DEFAULT 0,                -- 0~100 퍼센트
  input_params    JSONB,                                 -- 입력 파라미터
  result          JSONB,                                 -- 결과 데이터
  error_message   TEXT,
  media_asset_id  UUID REFERENCES media_assets(id),      -- 관련 미디어 (nullable)
  started_at      TIMESTAMPTZ,
  completed_at    TIMESTAMPTZ,
  created_at      TIMESTAMPTZ NOT NULL DEFAULT NOW()
);

-- 인덱스
CREATE INDEX idx_ai_jobs_organization_id ON ai_jobs(organization_id);
CREATE INDEX idx_ai_jobs_user_id ON ai_jobs(user_id);
CREATE INDEX idx_ai_jobs_status ON ai_jobs(status);

-- RLS
ALTER TABLE ai_jobs ENABLE ROW LEVEL SECURITY;
ALTER TABLE ai_jobs FORCE ROW LEVEL SECURITY;

CREATE POLICY tenant_isolation ON ai_jobs
  USING (organization_id = current_setting('app.current_org_id')::uuid);

CREATE POLICY sa_bypass ON ai_jobs
  USING (current_setting('app.user_role', true) = 'SYSTEM_ADMIN');
```

### 4.8 analytics_snapshots — 성과 스냅샷

> RLS 적용. 플랫폼별 일간 성과 데이터 저장. 대시보드(F06) Phase 1-B 확장에 필요.

```sql
CREATE TABLE analytics_snapshots (
  id              UUID PRIMARY KEY DEFAULT uuid_generate_v7(),
  organization_id UUID NOT NULL REFERENCES organizations(id),
  channel_id      UUID NOT NULL REFERENCES channels(id),
  platform        platform_type NOT NULL,
  snapshot_date   DATE NOT NULL,
  metrics         JSONB NOT NULL DEFAULT '{}',         -- 성과 지표
  created_at      TIMESTAMPTZ NOT NULL DEFAULT NOW(),

  CONSTRAINT uq_analytics_snapshot
    UNIQUE(channel_id, snapshot_date)
);

-- 인덱스
CREATE INDEX idx_analytics_snapshots_organization_id ON analytics_snapshots(organization_id);
CREATE INDEX idx_analytics_snapshots_channel_id ON analytics_snapshots(channel_id);
CREATE INDEX idx_analytics_snapshots_snapshot_date ON analytics_snapshots(snapshot_date DESC);
CREATE INDEX idx_analytics_snapshots_platform ON analytics_snapshots(platform);
CREATE INDEX idx_analytics_snapshots_metrics ON analytics_snapshots USING GIN(metrics);

-- 복합 인덱스 (대시보드 성과 조회)
CREATE INDEX idx_analytics_org_date
  ON analytics_snapshots(organization_id, snapshot_date DESC);

-- RLS
ALTER TABLE analytics_snapshots ENABLE ROW LEVEL SECURITY;
ALTER TABLE analytics_snapshots FORCE ROW LEVEL SECURITY;

CREATE POLICY tenant_isolation ON analytics_snapshots
  USING (organization_id = current_setting('app.current_org_id')::uuid);

CREATE POLICY sa_bypass ON analytics_snapshots
  USING (current_setting('app.user_role', true) = 'SYSTEM_ADMIN');
```

**`metrics` JSONB 스키마:**

```json
{
  "followers": 15000,
  "followersDelta": 120,
  "reach": 45000,
  "impressions": 82000,
  "engagementRate": 4.2,
  "likes": 1200,
  "comments": 340,
  "shares": 89,
  "views": 23000
}
```

---

## 5. Phase 2 테이블 (4개)

### 5.1 media_assets — 미디어 파일

> RLS 적용. MinIO 파일 메타데이터 관리.

```sql
CREATE TABLE media_assets (
  id              UUID PRIMARY KEY DEFAULT uuid_generate_v7(),
  organization_id UUID NOT NULL REFERENCES organizations(id),
  file_name       VARCHAR(255) NOT NULL,
  original_name   VARCHAR(255) NOT NULL,
  mime_type       VARCHAR(100) NOT NULL,
  media_type      media_type NOT NULL,
  file_size       BIGINT NOT NULL,                     -- bytes
  storage_path    VARCHAR(1024) NOT NULL,              -- MinIO 경로
  thumbnail_url   VARCHAR(1024),
  duration        INT,                                 -- 초 (영상/오디오)
  resolution      VARCHAR(20),                         -- 1920x1080
  tags            VARCHAR(100)[] NOT NULL DEFAULT '{}',
  folder_id       UUID,                                -- 폴더 (아래 media_folders 참조)
  version         INT NOT NULL DEFAULT 1,
  created_by      UUID NOT NULL REFERENCES users(id),
  created_at      TIMESTAMPTZ NOT NULL DEFAULT NOW(),
  updated_at      TIMESTAMPTZ NOT NULL DEFAULT NOW(),
  deleted_at      TIMESTAMPTZ
);

-- 인덱스
CREATE INDEX idx_media_assets_organization_id ON media_assets(organization_id);
CREATE INDEX idx_media_assets_media_type ON media_assets(media_type);
CREATE INDEX idx_media_assets_folder_id ON media_assets(folder_id);
CREATE INDEX idx_media_assets_tags ON media_assets USING GIN(tags);
CREATE INDEX idx_media_assets_created_at ON media_assets(created_at DESC);
CREATE INDEX idx_media_assets_created_by ON media_assets(created_by);

-- RLS
ALTER TABLE media_assets ENABLE ROW LEVEL SECURITY;
ALTER TABLE media_assets FORCE ROW LEVEL SECURITY;

CREATE POLICY tenant_isolation ON media_assets
  USING (organization_id = current_setting('app.current_org_id')::uuid);

CREATE POLICY sa_bypass ON media_assets
  USING (current_setting('app.user_role', true) = 'SYSTEM_ADMIN');
```

**미디어 폴더 참고:** API의 `GET /media/folders`, `POST /media/folders` 엔드포인트를 지원하기 위해 `media_assets.folder_id`는 아래 간이 테이블로 관리한다. Phase 2 마이그레이션(013)에 포함.

```sql
-- 미디어 폴더 (media_assets 부속 — 별도 카운트 X)
CREATE TABLE media_folders (
  id              UUID PRIMARY KEY DEFAULT uuid_generate_v7(),
  organization_id UUID NOT NULL REFERENCES organizations(id),
  name            VARCHAR(200) NOT NULL,
  parent_id       UUID REFERENCES media_folders(id),    -- 자기 참조 (트리 구조)
  created_at      TIMESTAMPTZ NOT NULL DEFAULT NOW()
);

CREATE INDEX idx_media_folders_organization_id ON media_folders(organization_id);
CREATE INDEX idx_media_folders_parent_id ON media_folders(parent_id);

ALTER TABLE media_assets ADD CONSTRAINT fk_media_assets_folder
  FOREIGN KEY (folder_id) REFERENCES media_folders(id);

-- RLS
ALTER TABLE media_folders ENABLE ROW LEVEL SECURITY;
ALTER TABLE media_folders FORCE ROW LEVEL SECURITY;

CREATE POLICY tenant_isolation ON media_folders
  USING (organization_id = current_setting('app.current_org_id')::uuid);

CREATE POLICY sa_bypass ON media_folders
  USING (current_setting('app.user_role', true) = 'SYSTEM_ADMIN');
```

### 5.2 content_media_assets — 콘텐츠-미디어 다대다

> RLS 적용. 콘텐츠에 첨부된 미디어 연결.

```sql
CREATE TABLE content_media_assets (
  id              UUID PRIMARY KEY DEFAULT uuid_generate_v7(),
  content_id      UUID NOT NULL REFERENCES contents(id) ON DELETE CASCADE,
  media_asset_id  UUID NOT NULL REFERENCES media_assets(id) ON DELETE CASCADE,
  organization_id UUID NOT NULL REFERENCES organizations(id),
  sort_order      INT NOT NULL DEFAULT 0,
  created_at      TIMESTAMPTZ NOT NULL DEFAULT NOW(),

  CONSTRAINT uq_content_media_assets
    UNIQUE(content_id, media_asset_id)
);

-- 인덱스
CREATE INDEX idx_content_media_assets_content_id ON content_media_assets(content_id);
CREATE INDEX idx_content_media_assets_media_asset_id ON content_media_assets(media_asset_id);
CREATE INDEX idx_content_media_assets_organization_id ON content_media_assets(organization_id);

-- RLS
ALTER TABLE content_media_assets ENABLE ROW LEVEL SECURITY;
ALTER TABLE content_media_assets FORCE ROW LEVEL SECURITY;

CREATE POLICY tenant_isolation ON content_media_assets
  USING (organization_id = current_setting('app.current_org_id')::uuid);

CREATE POLICY sa_bypass ON content_media_assets
  USING (current_setting('app.user_role', true) = 'SYSTEM_ADMIN');
```

### 5.3 calendar_events — 콘텐츠 캘린더

> RLS 적용. 예약 게시 + 공휴일/기념일 관리.

```sql
CREATE TABLE calendar_events (
  id              UUID PRIMARY KEY DEFAULT uuid_generate_v4(),
  organization_id UUID NOT NULL REFERENCES organizations(id),
  content_id      UUID REFERENCES contents(id),        -- nullable (공휴일은 콘텐츠 없음)
  event_type      calendareventtype NOT NULL,           -- ENUM (v1.2 VARCHAR → ENUM 변경)
  title           VARCHAR(255) NOT NULL,
  description     TEXT,                                 -- 이벤트 설명 (v1.3 추가)
  event_date      DATE NOT NULL,
  scheduled_at    TIMESTAMPTZ,
  platform        VARCHAR(50),                          -- 플랫폼명 (문자열)
  status          VARCHAR(20) NOT NULL,
  is_holiday      BOOLEAN NOT NULL DEFAULT FALSE,       -- 공휴일 여부 (v1.3 추가)
  is_recurring    BOOLEAN NOT NULL DEFAULT FALSE,       -- 반복 이벤트 여부 (v1.3 추가)
  color           VARCHAR(20),                          -- 캘린더 색상 (v1.3 추가)
  created_by      UUID REFERENCES users(id),
  created_at      TIMESTAMPTZ NOT NULL DEFAULT NOW(),
  updated_at      TIMESTAMPTZ NOT NULL DEFAULT NOW()
);

-- 인덱스
CREATE INDEX idx_calendar_events_organization_id ON calendar_events(organization_id);
CREATE INDEX idx_calendar_events_event_date ON calendar_events(event_date);
CREATE INDEX idx_calendar_events_content_id ON calendar_events(content_id);
CREATE INDEX idx_calendar_events_event_type ON calendar_events(event_type);

-- 복합 인덱스 (월별 캘린더 조회)
CREATE INDEX idx_calendar_events_org_date
  ON calendar_events(organization_id, event_date);

-- RLS
ALTER TABLE calendar_events ENABLE ROW LEVEL SECURITY;
ALTER TABLE calendar_events FORCE ROW LEVEL SECURITY;

CREATE POLICY tenant_isolation ON calendar_events
  USING (organization_id = current_setting('app.current_org_id')::uuid);

CREATE POLICY sa_bypass ON calendar_events
  USING (current_setting('app.user_role', true) = 'SYSTEM_ADMIN');
```

### 5.4 search_index_configs — Meilisearch 동기화 설정

> RLS 미적용. 시스템 설정 테이블.

```sql
CREATE TABLE search_index_configs (
  id           UUID PRIMARY KEY DEFAULT uuid_generate_v7(),
  index_name   VARCHAR(100) NOT NULL UNIQUE,           -- contents, comments, media
  source_table VARCHAR(100) NOT NULL,
  sync_fields  JSONB NOT NULL DEFAULT '[]',            -- 동기화 대상 컬럼
  last_synced_at TIMESTAMPTZ,
  is_active    BOOLEAN NOT NULL DEFAULT TRUE,
  created_at   TIMESTAMPTZ NOT NULL DEFAULT NOW(),
  updated_at   TIMESTAMPTZ NOT NULL DEFAULT NOW()
);
```

---

## 6. Phase 3 테이블 (1개)

### 6.1 reports — 리포트

> RLS 적용. 운영 리포트 (AI 생성 + 수동 편집).

```sql
CREATE TABLE reports (
  id              UUID PRIMARY KEY DEFAULT uuid_generate_v7(),
  organization_id UUID NOT NULL REFERENCES organizations(id),
  title           VARCHAR(255) NOT NULL,
  period          report_period NOT NULL,
  period_start    DATE NOT NULL,
  period_end      DATE NOT NULL,
  status          reportstatus NOT NULL DEFAULT 'DRAFT',   -- ENUM (v1.2 VARCHAR → ENUM 변경)
  content         JSONB NOT NULL DEFAULT '{}',           -- 리포트 본문 섹션
  pdf_url         VARCHAR(1024),                         -- 생성된 PDF 경로
  generated_by    VARCHAR(100),                          -- AI 모델명 (null이면 수동)
  created_by      UUID NOT NULL REFERENCES users(id),
  finalized_at    TIMESTAMPTZ,
  created_at      TIMESTAMPTZ NOT NULL DEFAULT NOW(),
  updated_at      TIMESTAMPTZ NOT NULL DEFAULT NOW()
);

-- 인덱스
CREATE INDEX idx_reports_organization_id ON reports(organization_id);
CREATE INDEX idx_reports_period ON reports(period);
CREATE INDEX idx_reports_status ON reports(status);
CREATE INDEX idx_reports_created_at ON reports(created_at DESC);

-- RLS
ALTER TABLE reports ENABLE ROW LEVEL SECURITY;
ALTER TABLE reports FORCE ROW LEVEL SECURITY;

CREATE POLICY tenant_isolation ON reports
  USING (organization_id = current_setting('app.current_org_id')::uuid);

CREATE POLICY sa_bypass ON reports
  USING (current_setting('app.user_role', true) = 'SYSTEM_ADMIN');
```

---

## 7. ERD 다이어그램

```
┌─────────────────────────────────────────────────────────────────────────────────┐
│                          PubliSync ERD — Phase 전체 (32개 테이블)               │
│                                                                                 │
│  ■ Phase 1-A (18)   □ Phase 1-B (9)   ▲ Phase 2 (4)   ○ Phase 3 (1)          │
└─────────────────────────────────────────────────────────────────────────────────┘

  ┌──────────────────┐                              ┌──────────────────┐
  │  ■ agencies       │──────────────────────────────│  ■ organizations  │
  │──────────────────│  1:N (agency_id FK)          │──────────────────│
  │  id (PK)         │                              │  id (PK)         │
  │  name            │                              │  name, slug (UQ) │
  │  contact_email   │                              │  plan, status    │
  │  is_active       │                              │  agency_id (FK)  │
  └──────────────────┘                              │  settings (JSONB)│
                                                    └────────┬─────────┘
                                                             │
          ┌─────────────────────┬────────────────────────────┼────────────────────┐
          │                     │                            │                     │
          ▼                     ▼                            ▼                     ▼
┌─────────────────┐  ┌────────────────────┐     ┌─────────────────┐   ┌─────────────────┐
│  ■ users         │  │  ■ user_organizations│     │  ■ channels      │   │  ■ workspaces    │
│─────────────────│  │────────────────────│     │─────────────────│   │─────────────────│
│  id (PK)        │  │  user_id (FK)      │     │  id (PK)        │   │  id (PK)        │
│  email (UQ)     │  │  organization_id   │     │  organization_id│←R │  organization_id│←R
│  status         │  │  role (user_role)  │     │  platform       │   │  name           │
│  preferences    │  │  is_primary        │     │  status, tokens │   └─────────────────┘
│  organization_id│  └────────────────────┘     └────────┬────────┘
└───────┬─────────┘                                      │
        │                                        ┌───────┴─────────────────┐
        │    ┌─────────────────────┐             │                          │
        │    │  ■ invitations       │             ▼                          ▼
        │    │─────────────────────│  ┌─────────────────────────┐  ┌────────────────────┐
        │    │  email, role        │  │  ■ contents              │  │  ■ publish_results  │
        │    │  organization_id   │  │─────────────────────────│  │────────────────────│
        │    │  token_hash, status│  │  id (PK)                │  │  content_id (FK)   │
        │    │  invited_by (FK)   │  │  organization_id        │←R│  channel_id (FK)   │
        │    └─────────────────────┘  │  title, body            │  │  status, metrics   │
        │                             │  status (content_status)│  │  platform_post_id  │
        │    ┌─────────────────────┐  │  author_id (FK→users)  │  └────────────────────┘
        │    │  ■ password_reset    │  │  platform_contents (J) │
        │    │    _tokens           │  │  metadata (JSONB)      │
        │    │─────────────────────│  │  ai_generated (JSONB)  │
        │    │  user_id (FK)       │  │  search_vector (tsv)   │
        │    │  token_hash         │  └───────┬──────┬─────────┘
        │    │  expires_at         │          │      │
        │    └─────────────────────┘          │      └─────────────────┐
        │                                     ▼                        ▼
        │    ┌───────────────────┐   ┌───────────────────────┐  ┌────────────────────┐
        │    │  ■ content_versions│   │  ■ approval_requests   │  │ ■ approval_workflows│
        │    │───────────────────│   │───────────────────────│  │────────────────────│
        │    │  content_id (FK)  │   │  content_id (FK)      │  │  organization_id   │←R
        │    │  version (UQ)     │   │  workflow_id (FK)─────│──│  steps (JSONB)     │
        │    │  title, body      │   │  status, requested_by │  └────────────────────┘
        │    └───────────────────┘   └───────────┬───────────┘
        │                                        ▼
        │                            ┌───────────────────────┐
        │                            │  ■ approval_histories  │
        │                            │───────────────────────│
        │                            │  request_id (FK)      │
        │                            │  action (ENUM)        │
        │                            │  reviewer_id (FK)     │
        │                            └───────────────────────┘

┌───────────────────┐  ┌───────────────────┐  ┌───────────────────┐
│  □ comments        │  │  □ notifications   │  │  □ audit_logs      │
│───────────────────│  │───────────────────│  │───────────────────│
│  organization_id  │  │  organization_id  │  │  organization_id  │
│  content_id (FK)  │  │  user_id (FK)     │  │  actor_id (FK)    │
│  channel_id (FK)  │  │  type, channel    │  │  action (ENUM)    │
│  parent_comment_id│  │  is_read          │  │  changes (JSONB)  │
│  sentiment, status│  │  payload (JSONB)  │  │  ── 월별 파티션 ──│
│  search_vector    │  └───────────────────┘  └───────────────────┘
└───────────────────┘

┌───────────────────┐  ┌────────────────────────┐  ┌───────────────────┐
│  □ reply_templates │  │  □ notification_settings │  │  □ ai_usage_logs   │
│───────────────────│  │────────────────────────│  │───────────────────│
│  organization_id  │  │  organization_id       │  │  organization_id  │
│  category         │  │  user_id (FK)          │  │  task_type (ENUM) │
│  content          │  │  channels (JSONB)      │  │  model, tokens    │
│  variables[]      │  │  push_subscription     │  │  estimated_cost   │
└───────────────────┘  └────────────────────────┘  └───────────────────┘

┌────────────────────────┐
│  □ analytics_snapshots  │
│────────────────────────│
│  organization_id       │
│  channel_id (FK)       │
│  snapshot_date         │
│  metrics (JSONB)       │
└────────────────────────┘

┌───────────────────┐  ┌─────────────────────────┐  ┌───────────────────┐
│  ▲ media_assets    │  │  ▲ content_media_assets   │  │  ▲ calendar_events │
│───────────────────│  │─────────────────────────│  │───────────────────│
│  organization_id  │  │  content_id (FK)        │  │  organization_id  │
│  file_name        │  │  media_asset_id (FK)    │  │  content_id (FK)  │
│  media_type       │  │  sort_order             │  │  event_date       │
│  storage_path     │  └─────────────────────────┘  └───────────────────┘
│  tags[]           │
└───────────────────┘

┌───────────────────┐
│  ○ reports         │
│───────────────────│
│  organization_id  │
│  title, period    │
│  content (JSONB)  │
│  pdf_url          │
└───────────────────┘

■ 전역 (RLS 미적용): users, organizations, agencies, roles, refresh_tokens,
                       user_organizations, invitations, password_reset_tokens,
                       system_announcements
※ search_index_configs (Phase 2): 시스템 설정 — 별도 FK 없음
```

**관계 요약:**

| 관계 | 설명 |
|---|---|
| agencies → organizations | 1:N (수탁업체 → 위탁기관) |
| users ↔ organizations | M:N (via user_organizations) |
| organizations → channels | 1:N |
| organizations → contents | 1:N (RLS 격리) |
| contents → content_versions | 1:N |
| contents → publish_results | 1:N (채널별 게시 결과) |
| contents → approval_requests | 1:N |
| approval_workflows → approval_requests | 1:N |
| approval_requests → approval_histories | 1:N |
| channels → comments | 1:N |
| contents → comments | 1:N (nullable) |
| comments → comments | 1:N (parent_comment_id, 대댓글) |
| contents ↔ media_assets | M:N (via content_media_assets) |
| channels → analytics_snapshots | 1:N |
| users → notifications | 1:N |
| users → notification_settings | 1:1 (per organization) |
| users → invitations | 1:N (invited_by) |

---

## 8. 인덱스 전략

### 8.1 인덱스 유형별 정리

| 유형 | 대상 | 예시 |
|---|---|---|
| **FK 인덱스** | 모든 `*_id` FK 컬럼 | `idx_contents_organization_id`, `idx_contents_author_id` |
| **복합 인덱스** | 빈번한 필터 + 정렬 조합 | `contents(organization_id, status, created_at DESC)` |
| **부분 인덱스** | 특정 조건의 행만 인덱싱 | `comments WHERE sentiment = 'DANGEROUS'` |
| **GIN 인덱스** | JSONB, 배열, tsvector | `contents.metadata`, `contents.platforms`, `search_vector` |
| **UNIQUE** | 비즈니스 유니크 제약 | `channels(org_id, platform, account_id)` |

### 8.2 핵심 복합 인덱스

```sql
-- 콘텐츠 목록 (가장 빈번한 쿼리)
CREATE INDEX idx_contents_org_status_created
  ON contents(organization_id, status, created_at DESC);

-- 예약 게시 스캔 (1분 주기 Beat)
CREATE INDEX idx_contents_scheduled_pending
  ON contents(scheduled_at)
  WHERE status = 'SCHEDULED' AND deleted_at IS NULL;

-- 승인 대기 목록
CREATE INDEX idx_approval_requests_org_status_created
  ON approval_requests(organization_id, status, created_at DESC);

-- 댓글 필터 (플랫폼 + 감성)
CREATE INDEX idx_comments_org_platform_sentiment
  ON comments(organization_id, platform, sentiment);

-- 위험 댓글 빠른 조회
CREATE INDEX idx_comments_dangerous
  ON comments(organization_id, created_at DESC)
  WHERE sentiment = 'DANGEROUS';

-- 미읽음 알림
CREATE INDEX idx_notifications_user_unread
  ON notifications(user_id, created_at DESC)
  WHERE is_read = FALSE;

-- 감사 로그 (기관 + 액션 + 시간)
CREATE INDEX idx_audit_logs_org_action_created
  ON audit_logs(organization_id, action, created_at DESC);

-- 대시보드 성과 조회
CREATE INDEX idx_analytics_org_date
  ON analytics_snapshots(organization_id, snapshot_date DESC);

-- AI 비용 월별 집계
CREATE INDEX idx_ai_usage_logs_org_month
  ON ai_usage_logs(organization_id, date_trunc('month', created_at));
```

### 8.3 신규 테이블 인덱스 (v1.1 추가)

```sql
-- user_organizations: 사용자별 소속 기관 조회
CREATE INDEX idx_user_organizations_user_id ON user_organizations(user_id);
CREATE INDEX idx_user_organizations_organization_id ON user_organizations(organization_id);

-- invitations: 이메일/상태별 조회
CREATE INDEX idx_invitations_email ON invitations(email);
CREATE INDEX idx_invitations_organization_id ON invitations(organization_id);
CREATE INDEX idx_invitations_status ON invitations(status) WHERE status = 'PENDING';

-- password_reset_tokens: 토큰 조회
CREATE UNIQUE INDEX idx_password_reset_tokens_hash ON password_reset_tokens(token_hash);

-- agencies: 활성 수탁업체 조회
CREATE INDEX idx_agencies_is_active ON agencies(is_active) WHERE is_active = TRUE;

-- system_announcements: 활성 공지 조회
CREATE INDEX idx_system_announcements_active
  ON system_announcements(publish_at DESC) WHERE is_active = TRUE;

-- comments: 전문 검색
CREATE INDEX idx_comments_search_vector ON comments USING GIN(search_vector);
```

### 8.4 tsvector 검색 인덱스

```sql
-- 콘텐츠 전문 검색 (Phase 1-A)
CREATE INDEX idx_contents_search_vector
  ON contents USING GIN(search_vector);

-- 댓글 전문 검색 (Phase 1-B)
CREATE INDEX idx_comments_search_vector
  ON comments USING GIN(search_vector);

-- 검색 쿼리 예시
SELECT * FROM contents
WHERE search_vector @@ plainto_tsquery('simple', '서울시 공공기관')
  AND organization_id = current_setting('app.current_org_id')::uuid
ORDER BY ts_rank(search_vector, plainto_tsquery('simple', '서울시 공공기관')) DESC
LIMIT 20;
```

---

## 9. RLS 정책 요약

### 9.1 RLS 적용 테이블 (23개, v1.3 갱신)

| # | 테이블 | Phase | 격리 키 |
|---|---|---|---|
| 1 | workspaces | 1-A | organization_id |
| 2 | channels | 1-A | organization_id |
| 3 | channel_histories | 1-A | organization_id |
| 4 | contents | 1-A | organization_id |
| 5 | content_versions | 1-A | organization_id |
| 6 | publish_results | 1-A | organization_id |
| 7 | approval_workflows | 1-A | organization_id |
| 8 | approval_requests | 1-A | organization_id |
| 9 | approval_histories | 1-A | organization_id |
| 10 | comments | 1-B | organization_id |
| 11 | notifications | 1-B | organization_id |
| 12 | notification_settings | 1-B | organization_id |
| 13 | reply_templates | 1-B | organization_id |
| 14 | audit_logs | 1-B | organization_id |
| 15 | ai_usage_logs | 1-B | organization_id |
| 16 | analytics_snapshots | 1-B | organization_id |
| 17 | ai_jobs | 1-B | organization_id |
| 18 | search_index_configs | 2 | organization_id |
| 19 | media_assets | 2 | organization_id |
| 20 | media_folders | 2 | organization_id |
| 21 | content_media_assets | 2 | organization_id |
| 22 | calendar_events | 2 | organization_id |
| 23 | reports | 3 | organization_id |

### 9.2 RLS 미적용 테이블 (8개, v1.3 갱신)

| # | 테이블 | 근거 |
|---|---|---|
| 1 | users | 여러 기관에 소속 가능 (M:N via user_organizations) |
| 2 | organizations | 기관 목록은 전역 조회 필요 |
| 3 | roles | 역할 정의는 시스템 전역 |
| 4 | refresh_tokens | user_id 기반 조회 (토큰 검증) |
| 5 | user_organizations | 다대다 매핑, user_id+org_id로 직접 필터 |
| 6 | invitations | 이메일 기반 조회, 특정 기관 종속 아님 |
| 7 | password_reset_tokens | user_id 기반 조회 (토큰 검증) |
| 8 | agencies | 수탁업체 목록은 전역 조회 필요 |

> **참고:** `system_announcements`도 RLS 미적용 (시스템 전역 공지). `search_index_configs`는 실제 구현에서 RLS 적용됨 (v1.2 문서와 차이).

### 9.3 RLS 정책 공통 패턴

```sql
-- 테넌트 격리 (모든 RLS 테이블에 적용)
CREATE POLICY tenant_isolation ON {table_name}
  USING (organization_id = current_setting('app.current_org_id')::uuid);

-- SA 역할 우회 (전체 데이터 접근)
CREATE POLICY sa_bypass ON {table_name}
  USING (current_setting('app.user_role', true) = 'SYSTEM_ADMIN');
```

### 9.4 Workspace 미들웨어 (세션 변수 설정)

```python
# FastAPI 미들웨어 → DB 세션 이벤트
async def set_rls_context(session: AsyncSession, org_id: UUID, role: str):
    await session.execute(
        text("SET app.current_org_id = :org_id"),
        {"org_id": str(org_id)}
    )
    await session.execute(
        text("SET app.user_role = :role"),
        {"role": role}
    )
    # SET (without LOCAL) — 세션 수명 동안 유지
```

---

## 10. 파티셔닝 전략

### 10.1 파티셔닝 대상

| 테이블 | 방식 | 파티션 키 | 근거 |
|---|---|---|---|
| `audit_logs` | RANGE (월별) | `created_at` | INSERT-ONLY, 고볼륨, 보관 정책 (3년) |
| `ai_usage_logs` | Phase 2에서 검토 | `created_at` | 데이터 증가 추이 관찰 후 결정 |

### 10.2 audit_logs 파티션 관리

```sql
-- 새 월 파티션 생성 (Celery Beat: 매월 1일)
CREATE TABLE audit_logs_2026_05 PARTITION OF audit_logs
  FOR VALUES FROM ('2026-05-01') TO ('2026-06-01');

-- 3년 초과 파티션 삭제
DROP TABLE IF EXISTS audit_logs_2023_04;  -- 2026년 기준 3년 전
```

**자동 파티션 관리 함수:**

```sql
CREATE OR REPLACE FUNCTION create_next_audit_partition() RETURNS void AS $$
DECLARE
  next_month DATE;
  partition_name TEXT;
  start_date TEXT;
  end_date TEXT;
BEGIN
  next_month := date_trunc('month', NOW()) + INTERVAL '1 month';
  partition_name := 'audit_logs_' || to_char(next_month, 'YYYY_MM');
  start_date := to_char(next_month, 'YYYY-MM-DD');
  end_date := to_char(next_month + INTERVAL '1 month', 'YYYY-MM-DD');

  EXECUTE format(
    'CREATE TABLE IF NOT EXISTS %I PARTITION OF audit_logs
     FOR VALUES FROM (%L) TO (%L)',
    partition_name, start_date, end_date
  );
END;
$$ LANGUAGE plpgsql;
```

### 10.3 커넥션 풀 설정

```python
# SQLAlchemy 2.0 async engine
engine = create_async_engine(
    DATABASE_URL,
    pool_size=20,          # 기본 커넥션 수
    max_overflow=10,       # 피크 시 추가 커넥션
    pool_timeout=30,       # 커넥션 대기 타임아웃 (초)
    pool_recycle=1800,     # 30분마다 커넥션 갱신
    pool_pre_ping=True,    # 사용 전 헬스 체크
    echo=False,
    connect_args={
        "server_settings": {
            "application_name": "publisync_api",
            "jit": "off"   # JIT 예측 불가 지연 방지
        }
    }
)
```

---

## 11. JSONB 필드 스키마

9개 주요 JSONB 필드의 상세 구조.

### 11.1 `users.preferences`

```json
{
  "language": "ko",
  "timezone": "Asia/Seoul",
  "theme": "light",
  "dashboardLayout": "default",
  "emailDigest": "daily",
  "defaultWorkspaceId": "uuid"
}
```

### 11.2 `organizations.settings`

```json
{
  "defaultLanguage": "ko",
  "timezone": "Asia/Seoul",
  "features": {
    "aiEnabled": true,
    "aiMonthlyQuota": 1000,
    "aiUsedCount": 0,
    "telegramEnabled": false,
    "webPushEnabled": false
  },
  "approval": {
    "requiredCount": 1,
    "enableAiReview": false
  }
}
```

### 11.3 `channels.metadata`

```json
{
  "followerCount": 15000,
  "profileUrl": "https://youtube.com/@channel",
  "profileImageUrl": "https://...",
  "capabilities": ["publish", "get_comments", "reply_comment", "get_analytics"]
}
```

### 11.4 `contents.metadata`

```json
{
  "hashtags": ["#공공기관", "#서울시"],
  "thumbnailUrl": "https://minio.../thumb.jpg",
  "ogImage": "https://...",
  "scheduledChannels": [
    { "channelId": "uuid", "platform": "YOUTUBE", "scheduledAt": "..." }
  ],
  "mediaIds": ["uuid1", "uuid2"]
}
```

### 11.5 `contents.ai_generated`

```json
{
  "hashtags": { "result": [...], "model": "gpt-4o-mini", "confidence": 0.87, "usedAt": "..." },
  "metaDescription": { "result": "...", "model": "gpt-4o-mini", "confidence": 0.92, "usedAt": "..." },
  "title": { "result": "...", "model": "gpt-4o-mini", "confidence": 0.85, "usedAt": "..." },
  "review": {
    "expression": { "status": "PASS", "message": "...", "details": [] },
    "privacy": { "status": "WARNING", "message": "...", "details": [] },
    "accessibility": { "status": "PASS", "message": "...", "details": [] },
    "copyright": { "status": "PASS", "message": "...", "details": [] },
    "overallStatus": "WARNING",
    "model": "gpt-4o",
    "usedAt": "..."
  }
}
```

### 11.6 `approval_workflows.steps`

```json
{
  "steps": [
    {
      "step": 1,
      "approverRole": "CLIENT_DIRECTOR",
      "approverIds": ["uuid1", "uuid2"],
      "requiredCount": 1
    }
  ],
  "fastTrack": {
    "enabled": false,
    "maxContentLength": 500,
    "allowedStatuses": ["DRAFT"]
  },
  "autoRejectConditions": {
    "maxPendingDays": 7,
    "notifyOnAutoReject": true
  }
}
```

### 11.7 `notifications.payload`

```json
{
  "contentId": "uuid",
  "channelId": "uuid",
  "commentId": "uuid",
  "message": "위험 댓글이 감지되었습니다.",
  "actionUrl": "/contents/uuid/comments"
}
```

### 11.8 `notification_settings.channels`

```json
{
  "web": { "enabled": true },
  "email": { "enabled": true },
  "telegram": {
    "enabled": false,
    "botToken": "...",
    "channels": [{ "id": "ch1", "name": "알림채널", "chatId": "-100123456" }]
  },
  "webPush": { "enabled": false }
}
```

### 11.9 `analytics_snapshots.metrics`

```json
{
  "followers": 15000,
  "followersDelta": 120,
  "reach": 45000,
  "impressions": 82000,
  "engagementRate": 4.2,
  "likes": 1200,
  "comments": 340,
  "shares": 89,
  "views": 23000
}
```

---

## 12. 마이그레이션 가이드

Alembic 마이그레이션을 스프린트와 정렬. `app/migrations/versions/` 디렉토리.

### 12.1 마이그레이션 목록

| # | 마이그레이션 | 스프린트 | 내용 |
|---|---|---|---|
| 001 | `001_initial_extensions` | S1 | `uuid-ossp`, `pgcrypto` 확장. ENUM 타입. `trigger_set_updated_at` 함수 |
| 002 | `002_auth_tables` | S2 | `users`, `roles`, `refresh_tokens`, `password_reset_tokens`, `invitations` 테이블 + 인덱스 |
| 003 | `003_system_announcements` | S3 | `agencies`, `organizations`, `workspaces`, `user_organizations`, `system_announcements` + RLS 정책 |
| 004 | `004_channels` | S4 | `channels`, `channel_histories` 테이블 + RLS 정책 + UNIQUE 제약 |
| 005 | `005_contents` | S5 | `contents`, `content_versions`, `publish_results` + RLS + tsvector 트리거 |
| 006 | `006_approvals` | S6 | `approval_workflows`, `approval_requests`, `approval_histories` + RLS |
| 007 | `007_comments_templates` | 1-B | `comments`, `reply_templates` + 감성 부분 인덱스 + RLS |
| 008 | `008_notifications` | 1-B | `notifications`, `notification_settings` + RLS |
| 009 | `009_audit_ai_usage` | 1-B | `audit_logs` (파티셔닝) + INSERT-ONLY 트리거 + `ai_usage_logs` + RLS |
| 010 | `010_media_library` | P2 | `media_assets`, `media_folders`, `content_media_assets` + RLS |
| 011 | `011_calendar_events` | P2 | `calendar_events` + RLS |
| 012 | `012_ai_jobs` | 1-B | `ai_jobs` + RLS |
| 013 | `013_workspaces` | 1-A | `workspaces` 보완 |
| 014 | `014_analytics_search` | 1-B | `analytics_snapshots`, `search_index_configs` + RLS + 대시보드 복합 인덱스 |
| 015 | `015_reports` | P3 | `reports` + RLS |
| 016 | `016_add_channel_last_comment_collected` | 1-B | `channels.last_comment_collected_at` 컬럼 추가 |
| 017 | `017_audit_logs_partitioning` | 1-B | `audit_logs` RANGE 파티셔닝 전환 + 파티션 RLS + 자동 생성/정리 함수 |

### 12.2 마이그레이션 실행

```bash
# 최신까지 마이그레이션
alembic upgrade head

# 특정 리비전까지
alembic upgrade 006_approvals

# 한 단계 롤백
alembic downgrade -1

# 현재 상태 확인
alembic current
```

### 12.3 RLS 수동 추가 주의사항

Alembic auto-generate는 RLS 정책을 감지하지 못하므로, 마이그레이션 파일에 수동으로 추가해야 함:

```python
# 마이그레이션 파일 예시
def upgrade():
    # 테이블 생성 (auto-generate)
    op.create_table('contents', ...)

    # RLS 수동 추가
    op.execute("ALTER TABLE contents ENABLE ROW LEVEL SECURITY")
    op.execute("ALTER TABLE contents FORCE ROW LEVEL SECURITY")
    op.execute("""
        CREATE POLICY tenant_isolation ON contents
          USING (organization_id = current_setting('app.current_org_id')::uuid)
    """)
    op.execute("""
        CREATE POLICY sa_bypass ON contents
          USING (current_setting('app.user_role', true) = 'SYSTEM_ADMIN')
    """)

def downgrade():
    op.execute("DROP POLICY IF EXISTS sa_bypass ON contents")
    op.execute("DROP POLICY IF EXISTS tenant_isolation ON contents")
    op.execute("ALTER TABLE contents DISABLE ROW LEVEL SECURITY")
    op.drop_table('contents')
```

---

## 13. SQLAlchemy Base 모델 패턴

### 13.1 Base 및 Mixin

```python
# app/models/base.py
from datetime import datetime
from uuid import UUID
from sqlalchemy import Column, DateTime, func, text
from sqlalchemy.dialects.postgresql import UUID as PG_UUID
from sqlalchemy.orm import DeclarativeBase, Mapped, mapped_column


class Base(DeclarativeBase):
    """SQLAlchemy 2.0 선언적 베이스"""
    pass


class BaseMixin:
    """공통 PK + 타임스탬프"""
    id: Mapped[UUID] = mapped_column(
        PG_UUID(as_uuid=True),
        primary_key=True,
        server_default=text("uuid_generate_v7()")
    )
    created_at: Mapped[datetime] = mapped_column(
        DateTime(timezone=True),
        server_default=func.now(),
        nullable=False
    )
    updated_at: Mapped[datetime] = mapped_column(
        DateTime(timezone=True),
        server_default=func.now(),
        onupdate=func.now(),
        nullable=False
    )


class TenantMixin:
    """멀티테넌트 격리 (RLS 대상 테이블에 적용)"""
    organization_id: Mapped[UUID] = mapped_column(
        PG_UUID(as_uuid=True),
        nullable=False,
        index=True
    )

    @classmethod
    def __declare_last__(cls):
        """RLS 힌트 — 마이그레이션에서 RLS 정책 수동 추가 필요"""
        cls.__rls_enabled__ = True


class SoftDeleteMixin:
    """소프트 삭제"""
    deleted_at: Mapped[datetime | None] = mapped_column(
        DateTime(timezone=True),
        nullable=True,
        default=None
    )

    @property
    def is_deleted(self) -> bool:
        return self.deleted_at is not None
```

### 13.2 모델 사용 예시

```python
# app/models/content.py
from app.models.base import Base, BaseMixin, TenantMixin, SoftDeleteMixin

class Content(Base, BaseMixin, TenantMixin, SoftDeleteMixin):
    __tablename__ = "contents"

    title: Mapped[str] = mapped_column(String(255), nullable=False)
    body: Mapped[str | None] = mapped_column(Text)
    status: Mapped[str] = mapped_column(
        Enum(ContentStatusEnum, name="content_status"),
        nullable=False,
        default="DRAFT"
    )
    author_id: Mapped[UUID] = mapped_column(
        PG_UUID(as_uuid=True),
        ForeignKey("users.id"),
        nullable=False
    )
    # ... (이하 컬럼)
```

### 13.3 세션 미들웨어 (RLS 컨텍스트 설정)

```python
# app/core/database.py
from sqlalchemy.ext.asyncio import AsyncSession, create_async_engine, async_sessionmaker
from sqlalchemy import event, text

engine = create_async_engine(settings.DATABASE_URL, **pool_config)
async_session_factory = async_sessionmaker(engine, expire_on_commit=False)


async def get_db_session(
    org_id: UUID | None = None,
    role: str | None = None
) -> AsyncGenerator[AsyncSession, None]:
    async with async_session_factory() as session:
        if org_id:
            await session.execute(
                text("SET LOCAL app.current_org_id = :org_id"),
                {"org_id": str(org_id)}
            )
        if role:
            await session.execute(
                text("SET LOCAL app.user_role = :role"),
                {"role": role}
            )
        try:
            yield session
            await session.commit()
        except Exception:
            await session.rollback()
            raise
```

---

## 14. 부록: 테이블 요약 매트릭스

### 14.1 전체 테이블 (34개, v2.0 갱신: +content_variants, variant_media)

| # | 테이블 | Phase | RLS | 소프트삭제 | 파티셔닝 | 주요 FK |
|---|---|---|:---:|:---:|:---:|---|
| 1 | users | 1-A | ✗ | ✓ | ✗ | organizations |
| 2 | organizations | 1-A | ✗ | ✗ | ✗ | agencies |
| 3 | roles | 1-A | ✗ | ✗ | ✗ | — |
| 4 | refresh_tokens | 1-A | ✗ | ✗ | ✗ | users |
| 5 | user_organizations | 1-A | ✗ | ✗ | ✗ | users, organizations |
| 6 | invitations | 1-A | ✗ | ✗ | ✗ | organizations, users |
| 7 | password_reset_tokens | 1-A | ✗ | ✗ | ✗ | users |
| 8 | agencies | 1-A | ✗ | ✗ | ✗ | — |
| 9 | system_announcements | 1-A | ✗ | ✗ | ✗ | users |
| 10 | workspaces | 1-A | ✓ | ✗ | ✗ | organizations |
| 11 | channels | 1-A | ✓ | ✗ | ✗ | organizations |
| 12 | channel_histories | 1-A | ✓ | ✗ | ✗ | channels, organizations, users |
| 13 | contents | 1-A | ✓ | ✓ | ✗ | organizations, users |
| 14 | content_versions | 1-A | ✓ | ✗ | ✗ | contents, organizations, users |
| 15 | publish_results | 1-A | ✓ | ✗ | ✗ | contents, organizations, channels |
| 16 | approval_workflows | 1-A | ✓ | ✗ | ✗ | organizations |
| 17 | approval_requests | 1-A | ✓ | ✗ | ✗ | contents, organizations, users |
| 18 | approval_histories | 1-A | ✓ | ✗ | ✗ | approval_requests, organizations, users |
| 18a | content_variants | 1-A | ✓ | ✗ | ✗ | contents, organizations, channels |
| 18b | variant_media | 1-A | ✓ | ✗ | ✗ | content_variants, media_assets, organizations |
| 19 | comments | 1-B | ✓ | ✗ | ✗ | organizations, contents, channels, comments |
| 20 | notifications | 1-B | ✓ | ✗ | ✗ | organizations, users |
| 21 | notification_settings | 1-B | ✓ | ✗ | ✗ | organizations, users |
| 22 | reply_templates | 1-B | ✓ | ✓ | ✗ | organizations, users |
| 23 | audit_logs | 1-B | ✓ | ✗ | ✓ (월별) | organizations, users |
| 24 | ai_usage_logs | 1-B | ✓ | ✗ | ✗ | organizations, users |
| 25 | ai_jobs | 1-B | ✓ | ✗ | ✗ | organizations, users, media_assets |
| 26 | analytics_snapshots | 1-B | ✓ | ✗ | ✗ | organizations, channels |
| 27 | media_assets | 2 | ✓ | ✓ | ✗ | organizations, users |
| 28 | media_folders | 2 | ✓ | ✗ | ✗ | organizations, media_folders |
| 29 | content_media_assets | 2 | ✓ | ✗ | ✗ | contents, media_assets, organizations |
| 30 | calendar_events | 2 | ✓ | ✗ | ✗ | organizations, contents, users |
| 31 | search_index_configs | 2 | ✓ | ✗ | ✗ | — |
| 32 | reports | 3 | ✓ | ✗ | ✗ | organizations, users |

### 14.2 수치 검증

| 항목 | v1.0 | v1.2 | v1.3 (실사) | 상태 |
|---|---|---|---|---|
| 총 테이블 수 | 24 | 31 | 34 | ✓ (+1: ai_jobs, +2: content_variants, variant_media) |
| Phase 1-A 테이블 | 12 | 18 | 20 | ✓ (+2: content_variants, variant_media) |
| Phase 1-B 테이블 | 6 | 8 | 9 | ✓ (+1: ai_jobs) |
| Phase 2 테이블 | 4 | 4 | 4 | ✓ |
| Phase 3 테이블 | 2 | 1 | 1 | ✓ |
| ENUM 타입 | 15 | 19 | 26 | ✓ (+7: 기존 +6, +1: mediaroletype) |
| RLS 적용 테이블 | 19 | 21 | 25 | ✓ (+4: ai_jobs, search_index_configs, content_variants, variant_media) |
| RLS 미적용 테이블 | 5 | 10 | 8 | ✓ (search_index_configs가 RLS 적용으로 이동, system_announcements 제외) |
| 마이그레이션 수 | 14 | 15 | 17 | ✓ (+2: 016_channel_column, 017_audit_partitioning) |
| JSONB 필드 스키마 | 7 | 10 | 10 | ✓ |

### 14.3 테이블별 상세 설명 (32개)

> 도메인별로 분류하여 각 테이블의 목적, 핵심 컬럼, 관계, 특수 기능을 기술한다.

---

#### 14.3.1 인증·사용자 관리 (4개)

| 테이블 | 목적 | 핵심 컬럼 | FK / 관계 | 비고 |
|---|---|---|---|---|
| **users** | 사용자 계정. 이메일 인증, 로그인 잠금(5회 실패→30분), 역할 할당 | `email`(unique), `password_hash`, `role`(UserRole), `status`(UserStatus), `failed_login_count`, `locked_until` | N:1 `organizations` | 소프트삭제(`deleted_at`) |
| **refresh_tokens** | JWT 리프레시 토큰 저장. 만료·폐기 추적, 사용자당 최대 5개 활성 토큰 제한 | `token_hash`(unique), `expires_at`, `is_revoked`, `device_info` | N:1 `users` | `cleanup_expired_tokens` 주기 정리 |
| **password_reset_tokens** | 일회성 비밀번호 재설정 토큰. 발급 후 만료 시간 내 사용 가능 | `token_hash`(unique), `expires_at`, `is_used` | N:1 `users` | — |
| **invitations** | 기관 초대. 이메일+역할 지정, 수락/만료/취소 상태 관리 | `email`, `role`(UserRole), `token_hash`(unique), `status`(InvitationStatus), `expires_at`, `accepted_at` | N:1 `organizations`, N:1 `users`(invited_by) | — |

#### 14.3.2 조직·워크스페이스 (5개)

| 테이블 | 목적 | 핵심 컬럼 | FK / 관계 | 비고 |
|---|---|---|---|---|
| **agencies** | 수탁업체(대행사). 다수 위탁기관을 관리하는 상위 조직 | `name`, `contact_email`, `contact_phone`, `is_active` | 1:N `organizations` | — |
| **organizations** | 위탁기관(공공기관). 워크스페이스·스토리지 쿼타·설정 관리 | `name`, `slug`(unique), `status`(OrgStatus), `plan`(OrgPlan), `settings`(JSONB), `storage_used_bytes`, `storage_quota_bytes`(50GB) | N:1 `agencies` | 소프트삭제(`deleted_at`) |
| **user_organizations** | 사용자↔기관 N:M 매핑. 기관별 역할 할당 | `role`(UserRole), `is_primary` | N:1 `users`, N:1 `organizations` | UNIQUE(`user_id`, `organization_id`) |
| **roles** | RBAC 역할 정의(SA/AM/AO/CD). 권한 목록을 JSONB로 저장 | `name`(unique), `permissions`(JSONB), `description` | — | 참조 전용 테이블 |
| **system_announcements** | 시스템 전체 공지사항. 전 사용자 대상 | `title`, `content`, `type`, `is_active`, `publish_at` | N:1 `users`(created_by) | RLS 미적용 (전역 데이터) |

#### 14.3.3 워크스페이스 (1개)

| 테이블 | 목적 | 핵심 컬럼 | FK / 관계 | 비고 |
|---|---|---|---|---|
| **workspaces** | 기관별 워크스페이스. 기관 컨텍스트 전환 단위 | `name`, `slug`, `is_active` | N:1 `organizations` | RLS 적용 |

#### 14.3.4 채널 관리 (2개)

| 테이블 | 목적 | 핵심 컬럼 | FK / 관계 | 비고 |
|---|---|---|---|---|
| **channels** | SNS 계정 연동. OAuth 토큰 Fernet 암호화 저장, 플랫폼별 상태 관리 | `platform`(PlatformType), `platform_account_id`, `name`, `status`(ChannelStatus), `access_token_enc`, `refresh_token_enc`, `token_expires_at`, `metadata`(JSONB), `last_comment_collected_at` | N:1 `organizations` | UNIQUE(`org_id`, `platform`, `platform_account_id`), RLS 적용 |
| **channel_histories** | 채널 연결/해제/토큰 갱신 이벤트 이력 (감사 추적) | `event_type`(ChannelEventType), `details`(JSONB) | N:1 `channels`, N:1 `organizations`, N:1 `users`(actor_id) | RLS 적용 |

#### 14.3.5 콘텐츠 관리 (4개)

| 테이블 | 목적 | 핵심 컬럼 | FK / 관계 | 비고 |
|---|---|---|---|---|
| **contents** | 콘텐츠 본문. 멀티 플랫폼 동시 게시, 예약, AI 생성 여부 추적 | `title`, `body`, `status`(ContentStatus), `platforms`(ARRAY), `channel_ids`(ARRAY), `scheduled_at`, `platform_contents`(JSONB), `ai_generated`(bool), `media_urls`(ARRAY), `search_vector`(TSVECTOR) | N:1 `users`(author_id), N:1 `organizations` | 소프트삭제, GIN 인덱스(검색), RLS 적용 |
| **content_versions** | 콘텐츠 수정 이력. 버전별 제목/본문 스냅샷 저장 | `content_id`, `version`(int), `title`, `body`, `metadata`(JSONB) | N:1 `contents`, N:1 `users`(changed_by) | RLS 적용 |
| **publish_results** | 채널별 게시 결과 추적. 성공/실패/재시도 횟수, 플랫폼 URL·조회수 | `status`(PublishResultStatus), `platform_post_id`, `platform_url`, `error_message`, `retry_count`, `views`, `likes`, `shares`, `comments_count` | N:1 `contents`, N:1 `channels`, N:1 `organizations` | RLS 적용 |
| **content_media_assets** | 콘텐츠↔미디어 에셋 연결(N:M). 정렬 순서 관리 | `sort_order`(int) | N:1 `contents`, N:1 `media_assets`, N:1 `organizations` | UNIQUE(`content_id`, `media_asset_id`), RLS 적용 |

#### 14.3.6 승인 워크플로우 (3개)

| 테이블 | 목적 | 핵심 컬럼 | FK / 관계 | 비고 |
|---|---|---|---|---|
| **approval_workflows** | 승인 워크플로우 템플릿. 단계·담당자 JSONB로 정의 | `name`, `steps`(JSONB), `is_active` | N:1 `organizations` | RLS 적용 |
| **approval_requests** | 콘텐츠별 승인 요청. 현재 단계·상태·긴급 여부 추적 | `current_step`(int), `status`(ApprovalStatus), `is_urgent`, `comment` | N:1 `contents`, N:1 `approval_workflows`, N:1 `users`(requested_by) | RLS 적용 |
| **approval_histories** | 승인/반려/수정요청 이력. 불변 기록 | `step`(int), `action`(ApprovalAction), `comment` | N:1 `approval_requests`, N:1 `users`(reviewer_id, nullable) | RLS 적용 |

#### 14.3.7 댓글·답변 (2개)

| 테이블 | 목적 | 핵심 컬럼 | FK / 관계 | 비고 |
|---|---|---|---|---|
| **comments** | SNS 댓글 수집(Celery Beat 5분). AI 감성분석·위험도 분류, 답글 관리 | `external_id`, `text`, `author_name`, `sentiment`(CommentSentiment), `sentiment_confidence`, `dangerous_level`, `keywords`(ARRAY), `status`(CommentStatus), `reply_text`, `reply_draft`, `replied_at` | N:1 `contents`(nullable), N:1 `channels`, 자기참조(`parent_comment_id`) | UNIQUE(`channel_id`, `external_id`), 위험 댓글 부분 인덱스, RLS 적용 |
| **reply_templates** | 댓글 답변 템플릿. 카테고리별 분류, 변수 치환 지원 | `category`, `name`, `content`, `variables`(ARRAY), `usage_count`, `is_active` | N:1 `users`(created_by), N:1 `organizations` | 소프트삭제(`deleted_at`), RLS 적용 |

#### 14.3.8 미디어 라이브러리 (2개)

| 테이블 | 목적 | 핵심 컬럼 | FK / 관계 | 비고 |
|---|---|---|---|---|
| **media_assets** | MinIO 저장 파일 메타데이터. 이미지/영상/음성/문서 4종, 썸네일·태그 관리 | `filename`, `original_filename`, `mime_type`, `media_type`(MediaType), `object_key`(unique), `file_size`, `duration`, `width`, `height`, `tags`(ARRAY), `thumbnail_url` | N:1 `media_folders`(nullable), N:1 `users`(created_by) | 소프트삭제, GIN 인덱스(`tags`), RLS 적용 |
| **media_folders** | 미디어 계층 폴더 구조. 자기참조로 하위 폴더 지원 | `name` | 자기참조(`parent_id`), N:1 `organizations` | UNIQUE(`org_id`, `parent_id`, `name`), RLS 적용 |

#### 14.3.9 알림 (2개)

| 테이블 | 목적 | 핵심 컬럼 | FK / 관계 | 비고 |
|---|---|---|---|---|
| **notifications** | 인앱 알림 대기열. 멀티 채널 발송(인앱/이메일/웹 푸시/텔레그램) | `type`(NotificationType), `channel`(NotificationChannel), `title`, `message`, `payload`(JSONB), `is_read`, `read_at`, `action_url` | N:1 `users`, N:1 `organizations` | 미읽음 부분 인덱스, RLS 적용 |
| **notification_settings** | 사용자별 알림 채널 설정. 웹 푸시 구독·텔레그램 챗 ID 저장 | `channels`(JSONB), `push_subscription`(JSONB), `telegram_chat_id` | N:1 `users`, N:1 `organizations` | UNIQUE(`org_id`, `user_id`), RLS 적용 |

#### 14.3.10 캘린더 (1개)

| 테이블 | 목적 | 핵심 컬럼 | FK / 관계 | 비고 |
|---|---|---|---|---|
| **calendar_events** | 콘텐츠 게시 일정·공휴일·기념일·커스텀 이벤트 통합 캘린더 | `event_type`(CalendarEventType), `title`, `description`, `event_date`, `scheduled_at`, `is_holiday`, `is_recurring`, `color`(hex) | N:1 `contents`(nullable, SET NULL), N:1 `users`(created_by) | 복합 인덱스(`org_id`, `event_date`), RLS 적용 |

#### 14.3.11 AI·분석 (3개)

| 테이블 | 목적 | 핵심 컬럼 | FK / 관계 | 비고 |
|---|---|---|---|---|
| **ai_usage_logs** | AI 요청별 토큰 소비·비용 추적. 모델별·작업유형별 집계 | `task_type`(AiTaskType), `model`, `prompt_tokens`, `completion_tokens`, `total_tokens`, `estimated_cost`(Numeric 10,6), `processing_time_ms`, `is_fallback` | N:1 `users`(nullable), N:1 `organizations` | 월별 과금 복합 인덱스, RLS 적용 |
| **ai_jobs** | 비동기 AI 작업(자막/숏폼/리포트/썸네일) 추적. 폴링 패턴 지원 | `job_type`(AiJobType), `status`(AiJobStatus), `progress`(0~100), `input_params`(JSONB), `result`(JSONB), `started_at`, `completed_at` | N:1 `users`, N:1 `media_assets`(nullable), N:1 `organizations` | RLS 적용 |
| **analytics_snapshots** | 채널별 성과 스냅샷 캐시. 대시보드 집계 데이터 저장 | `channel_id`, `date`, `metrics`(JSONB), `period` | N:1 `channels`, N:1 `organizations` | RLS 적용 |

#### 14.3.12 리포트 (1개)

| 테이블 | 목적 | 핵심 컬럼 | FK / 관계 | 비고 |
|---|---|---|---|---|
| **reports** | 운영 리포트(주간/월간/분기). AI 생성 또는 수동 작성, PDF 내보내기 지원 | `title`, `period`(ReportPeriod), `period_start`, `period_end`, `status`(ReportStatus), `content`(JSONB), `pdf_url`, `generated_by`("AI"/"MANUAL") | N:1 `users`(created_by), N:1 `organizations` | RLS 적용 |

#### 14.3.13 감사 로그 (1개)

| 테이블 | 목적 | 핵심 컬럼 | FK / 관계 | 비고 |
|---|---|---|---|---|
| **audit_logs** | 불변 감사 추적. 모든 시스템 액션 기록(INSERT-ONLY, UPDATE/DELETE 트리거 차단) | `action`(AuditAction), `resource_type`, `resource_id`, `changes`(JSONB — old→new), `ip_address`, `user_agent`, `request_id` | N:1 `users`(actor_id, nullable), N:1 `organizations` | **월별 RANGE 파티셔닝**(`created_at`), PL/pgSQL 자동 파티션 생성 함수, RLS 적용 |

#### 14.3.14 검색 (1개)

| 테이블 | 목적 | 핵심 컬럼 | FK / 관계 | 비고 |
|---|---|---|---|---|
| **search_index_configs** | Meilisearch 인덱스 설정. Phase 2에서 외부 검색 엔진 연동 시 사용 | `index_name`, `config`(JSONB), `is_active` | N:1 `organizations` | RLS 적용 |
